Alembic contains the initial production schema revision and all later schema changes. Docker and local Windows startup both run `alembic upgrade head` before starting the API. Existing legacy SQLite databases are reconciled by the explicit compatibility bridge in `app.seed`; fresh databases use the normal revision chain.
