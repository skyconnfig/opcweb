# 全局复利与踩坑日志

## 2026-09-17｜FollowTask 到期逾期提醒最小闭环

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 持久化 / 调度 / 验证
- 关键词：`FollowTask` `OVERDUE` `notification_events` `APScheduler` `幂等`
- 状态：已验证；未提交、未推送

### 背景与现象

FollowTask 已能创建和完成，但缺少到期检测、逾期状态持久化和可恢复的站内提醒事件。仅依赖进程内事件无法覆盖服务重启或错过 SSE 的场景。

### 根因

原调度器只处理采集计划，没有 FollowTask 到期 Worker；任务表也没有 `overdue_at`、`reminded_at` 和 `reminder_count` 字段，无法保证重复调度不重复通知。

### 解决办法

- 新增 `FollowTask` 到期字段和 `notification_events` 持久化表。
- 使用数据库条件更新将 `PENDING` 原子认领为 `OVERDUE`，并用 `follow_task_id + event_type` 唯一约束保证同一任务只产生一次逾期事件。
- 新增每 30 秒执行的 `follow-task-reminders` APScheduler 作业；提交事务后再向现有 SSE 事件总线广播，数据库记录仍是事实来源。
- 新增按项目/未读筛选提醒及标记已读 API；没有发送抖音消息或自动化外部写操作。

### 验证结果

- `tests/test_follow_task_reminders.py` 与 `tests/test_migrations.py`：5 passed。
- 全量后端：`151 passed`，仅 2 个 Alembic 配置弃用警告。
- `alembic check`：`No new upgrade operations detected`。
- 当前 SQLite 已升级到 `c9d0e1f2a3b4 (head)`，`follow_tasks` 和 `notification_events` 结构存在；现有 67 条任务均未到期，未生成提醒。
- 重启 API 后 `/health` 返回 running，OpenAPI 暴露提醒接口，未读提醒查询返回空列表；未修改前端。

### 剩余限制

- 当前只实现一次性逾期提醒；没有重复提醒策略、邮件/桌面通知或前端提醒中心。
- 站内实时广播依赖在线 SSE 客户端，离线期间通过提醒查询 API 读取持久化记录。
- 多租户鉴权仍沿用现有 API 边界，提醒接口的项目过滤不是独立授权系统。

## 2026-09-17｜前端静态资源失配与 API 无限加载修复

- 项目：opcweb / AI Lead Radar
- 类型：前端修复 / 启动脚本 / 验证
- 状态：已验证并推送

### 背景与现象

用户截图显示页面退化为浏览器默认样式，前端还可能长时间停留在“正在读取本地工作区”。现场确认 Next 开发进程在生产构建更新 `.next` 后仍引用旧的 CSS/JS 资源；前端请求也没有超时边界。

### 解决办法

- `start.ps1` 检查首页引用的全部 Next CSS/JS 资源，拒绝 404、空响应和错误的 HTML 响应；发现失配时只重启本项目 Web 进程，不触碰 API、数据库和抖音 Profile。
- 前端 API 请求增加 30 秒 AbortController 超时，并把超时转换为可见的中文错误提示，避免加载态无限等待。
- 重启 Web 后重新检查首页资源，再执行桌面、深链接、详情交互和移动端冒烟。

### 验证结果

- `web\npm run lint` 通过。
- `web\npm run build` 通过。
- `scripts\frontend_smoke_test.py --report data\acceptance\frontend-smoke-current.json` 通过：15 个视图、15 个 hash 路由、详情交互、移动端菜单和无横向溢出；console error 0、非 SSE 请求失败 0。
- API `8689/health` 和 Web `5173` 均返回 200。
- 提交 `cfb5449 fix: make frontend startup and API loading resilient` 已推送到 `origin/main`。

### 下次预防

执行 `npm run build` 后必须重新探测首页实际引用的资源；前端读取接口必须具备超时和可恢复错误态，不能只依赖端口监听或首页 HTTP 200。

## 2026-09-17｜前端回复审核一致性与构建后运行态修复

- 项目：opcweb / AI Lead Radar
- 类型：前端交互 / 构建运行态 / 响应式验证
- 状态：已验证；真实抖音发送仍需人工确认和平台现场验收

### 背景与现象

评论详情抽屉在回复已批准后仍可编辑，可能造成审核文本与实际发送文本不一致；回复队列还可能使用旧的前端草稿覆盖服务端已审核文本。执行前端构建后，旧 Next 开发进程也可能继续引用失效的 `.next` 资源，导致页面退化为无样式 HTML。

### 解决办法

- `CommentDrawer` 在 `APPROVED` 后锁定文本、禁止关闭抽屉的误操作，并提供详情错误重试入口。
- 评论分析/生成操作错误显示在当前抽屉内；回复队列发送和批量发送只使用服务端已批准文本，编辑提交期间锁定编辑控件。
- Agent 页面补充加载、刷新和空态；视频列表在窄屏下约束文本与元数据换行，避免内部横向溢出。
- 清理失配的本地 Next 构建目录后重新构建，并通过 `start.ps1` 让 HTML 与静态资源重新匹配。

### 验证结果

- `web\npm run lint` 通过。
- `web\npm run build` 通过，生成 `/`、`/_not-found` 和 API 路由。
- `scripts\frontend_smoke_test.py` 通过：15 个视图、路由切换、详情抽屉、移动端菜单、无横向溢出；console errors 0、非 SSE 请求失败 0。
- `pytest -q`：145 passed，2 个 Alembic 弃用警告。
- 当前运行态：Web `http://127.0.0.1:5173`，API `http://127.0.0.1:8689`；未提交、未推送。

### 下次预防

前端构建后必须重启或探测 Web 开发服务的实际 CSS/JS 资源；审核类流程必须以服务端已批准文本为发送唯一来源，不能信任可能残留的客户端草稿。

## 2026-09-17｜前后端功能对应审计补齐 LeadAssistantAgent

- 项目：opcweb / AI Lead Radar
- 类型：前后端契约 / 实现 / 验证
- 关键词：`LeadAssistantAgent` `POST /api/leads/{lead_id}/assistant` `text-only` `AgentRun`
- 状态：已验证（代码与本地运行态）；真实抖音平台链路仍按验收矩阵执行

### 背景与现象

前后端功能审计发现潜客抽屉原来调用 PersonaAgent 建议接口，但任务要求的销售跟进助手契约 `reply`、`reason`、`risk`、`next_action` 没有独立的 Agent、API 和前端展示。

### 解决办法

- 新增 `LeadAssistantAgent` 和文本提示词，只接收行业/业务字段、潜客字段、评论历史、知识库和人设；过滤媒体 URL/字段，不调用视觉能力。
- 新增 `POST /api/leads/{lead_id}/assistant`，加载真实项目、潜客、评论、知识库和人设，记录成功或失败的 `AgentRun`，并把建议持久化到潜客记录。
- 潜客详情改为调用该接口，展示回复、判断理由、风险提示和下一步动作；保留旧 PersonaAgent 兼容能力。
- 智能体页面增加 LeadAssistantAgent 的可观测入口，README 与文本模型架构文档同步更新。

### 验证结果

- 后端：`145 passed`，仅 2 个 Alembic 配置弃用警告。
- 前端：`npm run lint`、`npm run build` 通过。
- 运行态 OpenAPI 已暴露 `POST /api/leads/{lead_id}/assistant`；`/health`、`/ready` 和 Web 首页均返回 200。
- 前端只读冒烟：15 个视图、潜客/评论/视频/任务详情抽屉、移动端菜单均通过；console error 0，非 SSE 请求失败 0。
- 未提交、未推送；真实抖音安全验证、crawler 和回复 E2E 不因本项被误标为通过。

### 下次预防

新增 Agent 时必须同时核对：输入字段来源、纯文本边界、结构化输出契约、API 路由、前端展示、失败审计和持久化结果；旧 Agent 接口保留时要明确新旧职责，避免页面名称与实际调用不一致。

## 2026-09-17｜前端裸 HTML 修复后的再次构建与运行态验收

- 项目：opcweb / AI Lead Radar
- 类型：前端修复 / 构建 / 验证
- 关键词：`Next.js` `静态资源` `start.ps1` `frontend_smoke_test`
- 状态：已验证

### 背景与现象

用户提供的页面截图显示产品界面退化为浏览器默认样式，疑似 Next.js 页面引用的 CSS/JS 与运行中的开发服务构建产物不一致。

### 解决办法

- 通过 `start.ps1` 检查首页实际引用的全部 `/_next/static` CSS/JS 资源；构建后发现服务需要恢复时，仅重启本项目 Web 进程。
- 使用当前本地 API 和 Web 运行态重新打开页面，不修改 API、数据库或抖音登录 Profile。

### 验证结果

- `web\npm run lint` 通过。
- `web\npm run build` 通过。
- 构建后重启 Web，首页 CSS/JS 资源均可访问；Playwright 只读冒烟通过。
- 桌面端 15 个视图访问正常，console error 0，非 SSE 请求失败 0；移动端菜单打开/关闭和无横向溢出均通过。
- 当前 Web：`http://127.0.0.1:5173`；API：`http://127.0.0.1:8689`。

### 下次预防

执行 `npm run build` 后不要直接复用旧开发进程做页面验收；先运行 `start.ps1`，再检查首页实际引用资源并执行 `scripts\frontend_smoke_test.py`。

## 2026-09-17｜抖音账号前端状态自动复核与潜客抽屉可访问性

- 项目：opcweb / AI Lead Radar
- 类型：前端修复 / 验证
- 关键词：`DouyinConnectionView` `登录状态轮询` `潜客详情` `aria-dialog`
- 状态：已验证

### 背景与现象

抖音账号页只在进入页面或点击按钮时读取一次真实登录状态；用户在可见浏览器完成扫码或安全验证后，页面不会自动反映状态。潜客详情抽屉也缺少 dialog 语义，自动化和辅助技术无法稳定识别详情面板。

### 解决办法

- 为账号页增加 10 秒一次的真实 `/api/douyin/status` 复核，并用进行中锁避免轮询、手动检查和启动/关闭浏览器动作重叠。
- 为潜客详情抽屉补充 `role=dialog`、`aria-modal` 和稳定 `aria-label`；不改变后端接口和数据副作用。

### 验证结果

- `web/npm run lint` 通过。
- `web/npm run build` 通过。
- 账号页浏览器验证 12.5 秒收到 4 次状态请求，无 console error。
- 前端只读冒烟：15 个视图、移动端菜单、资源加载均通过；console error 0、非 SSE 请求失败 0。

### 下次预防

涉及外部登录态的前端页面，不能只验证首次加载；应验证状态变化可被轮询或用户主动刷新反映，并保证请求不会并发占用同一浏览器 Profile。

## 2026-09-17｜Next 开发服务复用旧构建导致前端裸 HTML

- 项目：opcweb / AI Lead Radar
- 类型：踩坑 / 修复 / 验证
- 关键词：`Next.js` `/_next/static/css` `npm run build` `开发服务`
- 状态：已验证

### 背景与现象

浏览器页面只显示浏览器默认按钮和文字，产品布局、颜色和图标全部丢失。

### 根因

在开发服务运行期间执行 `npm run build` 后，Next 的 `.next` 静态资源发生切换，旧开发进程仍引用已经不存在的 CSS/JS 文件，导致首页 HTML 为 200 但 `/_next/static/css/app/layout.css` 返回 404。

### 解决办法

- 重启当前仓库的 Web 开发服务，让 HTML 引用与 `.next` 静态资源重新匹配。
- `start.ps1` 新增实际 stylesheet 探针：只有首页和 CSS 资源都可访问时才复用现有 Web 进程；发现资源失配时只重启本项目自己的 Web 进程。

### 验证结果

- 首页 HTTP 200。
- 首页引用的 CSS HTTP 200，内容包含 `.product-shell` 和 `.sidebar`。
- Playwright headless 首页冒烟无浏览器控制台错误，产品壳层计算样式为 `display: flex`。
- `npm run lint`、`npm run build` 通过；构建后重启开发服务再次验证 CSS 仍为 200。

### 下次预防

执行生产构建后不要直接用旧开发进程做 UI 验收；先运行 `start.ps1` 或重启 Web，再检查 HTML 引用的每个 `/_next` 资源状态。

## 2026-09-17｜前端裸 HTML 的启动脚本恢复加固

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 验证
- 关键词：`Next.js` `start.ps1` `静态资源探针` `进程树`
- 状态：已验证

### 背景与现象

用户截图显示前端退化为浏览器默认样式，只剩 HTML 文本和按钮。HTTP 首页仍能返回 200，但问题可能来自旧 Next 开发进程继续引用已被 `npm run build` 替换的 `.next` CSS/JS 资源。

### 根因

执行生产构建会更新 `.next` 资源，而旧开发服务仍在运行时可能返回过期资源引用。原启动脚本在恢复路径只停止监听 5173 的子进程，未明确处理同一 Web 服务的 npm/Next 父进程树，存在旧进程残留风险。

### 解决办法

- 保留首页实际引用的所有 `/_next/static` CSS/JS 资源探针。
- 新增仅匹配当前仓库路径和 Next/npm dev 命令的 Web 进程清理函数；不触碰 API、数据库或抖音 Profile。
- 发现资源失配时先退出项目 Web 进程树，再由 `start.ps1` 重新启动开发服务。

### 验证结果

- 重启 Web 后首页和 6 个 Next 静态资源均返回 HTTP 200。
- Playwright 桌面端加载产品壳层为 `display: flex`，无 console error；移动端菜单可用且无横向溢出。
- `web\npm run lint` 通过，`web\npm run build` 通过。
- `start.ps1` 再次执行时正确复用健康服务；API 8689 未被重启。

### 下次预防

执行 `npm run build` 后必须通过 `start.ps1` 重新确认 Web 资源；不要只凭首页 HTTP 200 判断前端已正常加载。资源恢复时只按仓库路径和 Next/npm 命令清理，避免误杀其他项目进程。

## 文档作用

记录跨项目可复用的经验、失败原因、修复方案、验证证据和预防措施，让后续项目可以复用已经验证过的路径，减少重复踩坑。

## 记录原则

- 及时记录：问题解决后尽快补充，不依赖事后回忆。
- 事实优先：区分已验证结果、推测和待验证事项。
- 可复用：重点记录触发条件、具体做法和验证方式。
- 可检索：每条记录包含项目名、日期和关键词。
- 不记录密码、Token、私钥、完整连接串等敏感信息；使用占位符代替。

## 条目模板

复制以下模板新增日志条目，按时间倒序排列：

```markdown
## YYYY-MM-DD｜简短标题

- 项目：项目名
- 类型：经验 / 踩坑 / 决策 / 工具 / 验证
- 关键词：`keyword-a` `keyword-b`
- 状态：已验证 / 部分验证 / 待验证 / 已失效

### 背景与现象

描述当时要完成什么，以及实际发生了什么。

### 根因

记录已确认的原因；如果只是推测，明确标注“推测”。

### 解决办法

记录可以复用的命令、步骤、代码位置或判断方法。敏感值使用占位符。

### 验证结果

记录执行了什么验证、结果是什么、证据在哪里。

### 下次预防

说明以后如何更早发现或避免同类问题。

### 可复用资产

列出脚本、模板、文档或其他可复用文件的路径。
```

## 推荐记录范围

- 环境安装、版本兼容和启动方式
- 构建、测试、部署和回滚中的失败与修复
- 用户明确确认过的产品决策和目录约定
- 可复用的脚本、检查清单和验证命令
- 发现的限制、未解决问题和后续替代方案

## 不应记录的内容

- 未验证的猜测作为确定结论
- 密码、API Key、Cookie、私钥和真实生产凭据
- 与项目无关的长篇过程转录
- 没有复现条件、解决步骤或验证结果的空泛感想

## 2026-09-17｜opcweb 当前状态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`pytest` `Next.js` `端口` `真实验收` `纯文本 AI`
- 状态：部分验证

### 背景与现象

复核当前仓库能否继续开发和本地试用。代码仓库没有运行中的 API 或 Web 进程，历史 SQLite 数据仍保留，真实采集评论的覆盖状态均为 `partial`。

### 根因

当前只完成了代码级验证和此前保存的真实 smoke 证据；本次复核没有启动服务，也没有重新执行抖音登录、搜索和评论采集，因此不能把历史证据升级为当前运行验收。

### 解决办法

- 先用 `git status --short --branch`、端口检查和 SQLite 只读统计确认现场。
- 后端使用 `.venv\Scripts\python.exe -m pytest -q`；前端在 `web` 目录执行 `npm run lint` 和 `npm run build`。
- 真实试用前按 `docs\real-acceptance-matrix.md` 补齐系统入库关联、人工复核和失败上下文，不把 `partial` 当作完整覆盖。

### 验证结果

- 后端：127 passed，2 个 Alembic deprecation warnings。
- 前端：TypeScript lint 通过，Next.js production build 通过。
- `git diff --check` 通过；API 8689 和 Web 5173 在本次检查时均未监听。
- 本地数据库只读统计：4 个项目、106 个关键词、26 个视频、259 条评论、27 个潜客、262 次 Agent 运行；259 条评论覆盖均为 `partial`。

### 下次预防

代码检查通过后仍要单独执行 `start.ps1`、`/health`、`/ready`、首页和真实 Provider smoke；上线判断以真实证据包为准，而不是仅以测试、构建或截图为准。

### 可复用资产

- `docs\commercial-readiness.md`
- `docs\real-acceptance-matrix.md`
- `start.ps1`

## 2026-09-17｜本地服务与真实登录复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 决策
- 关键词：`start.ps1` `health` `Playwright` `LOGGED_IN` `Next.js`
- 状态：部分验证

### 背景与现象

为完成前后端对应关系复核，启动本地 API 与 Web，并检查页面、接口和持久化抖音登录状态。

### 根因

数据库中的历史 `content_provider` 设置指向 `Douyin Comments Crawler`，导致启动后默认 Provider 为 disconnected；这不是代码自动降级，而是本地持久化配置覆盖了 `.env` 默认值。

### 解决办法

- 通过 Provider 激活接口将本地当前源切换为 `Douyin Playwright`，不改生产代码。
- 用 `/ready`、`/health`、`/api/douyin/status` 和首页 HTTP 响应检查运行态。
- 逐项打开前端 14 个导航页面，检查页面标题和浏览器控制台错误。

### 验证结果

- API `8689`、Web `5173` 正常监听；`/ready` 成功，`/health` 为 `running`。
- `/api/douyin/status` 返回 `browser=running`、`login=LOGGED_IN`；持久化 Profile 被正确恢复。
- 首页 HTTP 200；14 个导航页面均能渲染，前端控制台错误为 0。
- 当前 `Douyin Comments Crawler` 仍为 disconnected，未伪造或降级为样例数据。

### 下次预防

启动后优先检查数据库持久化 Provider 是否覆盖 `.env`；真实验收必须同时记录当前 Provider、登录状态、任务 ID、真实视频/评论和人工复核结果。

### 可复用资产

- `start.ps1`
- `backend\app\main.py`
- `web\app\page.tsx`
- `docs\real-acceptance-matrix.md`

## 2026-09-17｜真实搜索复核遇到平台安全验证

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`douyin/search` `DOUYIN_VERIFICATION_REQUIRED` `DeepSeek` `E2E`
- 状态：待人工处理

### 背景与现象

在服务已启动、Provider 已切换为 Playwright、登录状态显示 `LOGGED_IN` 后，使用项目已有关键词调用真实 `/api/douyin/search`。

### 根因

抖音搜索页面返回需要人工完成安全验证，API 返回 `409 DOUYIN_VERIFICATION_REQUIRED`。登录态成功不代表当前搜索动作已通过平台验证；不能通过代码绕过验证码或风控。

### 解决办法

- 保留真实错误，不降级到 Mock、Demo 或硬编码结果。
- 在已打开的 Playwright 抖音浏览器中由用户人工完成平台要求的验证，完成后重新运行搜索与评论 smoke。
- 同时调用 `/api/settings/test-llm` 验证文本模型链路。

### 验证结果

- DeepSeek 文本连接测试成功，使用已保存的文本模型配置；未调用视觉模型。
- 真实抖音搜索被安全验证拦截，当前 E2E 仍不能判定为本次运行通过。

### 下次预防

把 `LOGGED_IN`、搜索成功、视频 DOM 结果和评论 ID/文本分别作为独立验收步骤；任何一步出现安全验证都暂停并交给用户处理。

### 可复用资产

- `scripts\douyin_smoke_test.py`
- `docs\real-acceptance-matrix.md`
- `backend\app\providers\douyin\playwright_provider.py`

## 2026-09-17｜安全验证页错误分类修复

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 验证
- 关键词：`DouyinVerificationRequired` `page_title` `selector drift`
- 状态：已验证

### 背景与现象

真实搜索在抖音“验证码中间页”上曾被报告为 `DOUYIN_SELECTOR_NOT_FOUND`，前端无法明确提示用户需要人工完成验证。

### 根因

安全验证页可能没有可读的 body 文本或可见验证 selector，但页面标题仍是明确的“验证码中间页”；旧逻辑只检查 DOM selector，漏掉了标题/URL 这一文本元数据证据。

### 解决办法

- 新增基于页面标题和 URL 的安全验证页识别。
- `_detect_login_status`、必需 selector 和集合 selector 均统一抛出 `DouyinVerificationRequired`。
- 未读取截图、Canvas 或挑战几何，也未绕过任何平台验证。
- 先补 2 个回归测试，再修改 Provider。

### 验证结果

- 定向测试：2 passed。
- 全量后端测试：129 passed，2 个 Alembic deprecation warnings。
- 重启服务后真实搜索现在稳定返回 `409 DOUYIN_VERIFICATION_REQUIRED`，错误分类正确。
- `/health` 仍为 `running`，登录状态仍为 `LOGGED_IN`，DeepSeek 文本连接仍成功。

### 下次预防

平台页面异常需区分登录、人工验证、页面加载失败和 selector 漂移；先记录真实页面 URL/标题，再决定错误类型，禁止用空结果或 Mock 掩盖。

### 可复用资产

- `backend\app\providers\douyin\playwright_provider.py`
- `tests\test_douyin_playwright_contract.py`

## 2026-09-17｜当前项目运行态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 状态复核
- 关键词：`health` `ready` `Next.js` `pytest` `Git` `商业化`
- 状态：部分验证

### 背景与现象

检查当前工作区、服务运行态、测试结果和远程同步状态，确认是否可以继续本地试用。

### 根因

当前代码级质量门槛已通过，但抖音真实搜索此前被平台安全验证页拦截；登录状态为 `LOGGED_IN` 不等于搜索链路已完成真实 E2E。商业化能力仍停留在单租户、小规模、人工确认跟进边界。

### 解决办法

- 只读检查三份全局文档、Git 工作区、监听端口、健康探针和 OpenAPI 路由。
- 用 `.venv\Scripts\python.exe -m pytest -q` 复核后端测试。
- 将 `/api\keywords` 误判为缺失接口的检查改为使用实际项目级路由 `/api\projects\{project_id\}/keywords`；不要把不存在的汇总路由当作功能失败。

### 验证结果

- API `8689`、Web `5173` 正在监听；`/health` 返回 `running`，`/ready` 通过。
- 抖音 Playwright 浏览器正在运行，持久化登录状态为 `LOGGED_IN`；DeepSeek 文本模型已配置。
- 后端测试 `129 passed`，仅有 2 个 Alembic deprecation warnings；Web 首页 HTTP 200。
- 当前分支 `main` 与 `origin/main` 同步，但工作区仍有 2 个代码文件修改和 4 个全局文档未跟踪，尚未提交/推送。

### 下次预防

本地可运行、接口可访问和代码测试通过，只能证明开发态健康；再次判断商业化前必须重新完成人工安全验证后的真实“关键词 → 视频 → 评论 → 入库 → 规则预筛 → 文本 LLM → 潜客池”链路，并补齐脱敏证据包。

## 2026-09-17｜前后端对应性补强与全量自动化验证

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 验证
- 关键词：`SSE` `AgentRun` `评论筛选` `抖音账号状态` `Next.js build`
- 状态：已验证（自动化）；真实抖音 E2E 仍待人工安全验证

### 背景与现象

功能矩阵复核发现任务中心虽展示“数据库 + SSE”，前端实际只有轮询；评论池缺少未分析、B 级和失败筛选；直接评论分析或回复生成失败且没有有效模型调用记录时，审计记录存在被默认标成成功的风险。

### 根因

前端尚未消费 `/api/events/stream`；失败记录辅助函数在没有 `last_call` 时默认 `success=True`；账号状态接口返回字段不足以完整展示账号和登录时间。

### 解决办法

- 任务中心接入可恢复 SSE，并保留轮询作为断线兜底。
- AgentRun 记录支持显式 `success/error`，直接 Agent 失败路径明确写入失败。
- 评论池补齐状态筛选；抖音状态接口补充账号展示所需的非敏感字段。

### 验证结果

- 后端全量测试：`130 passed`，2 个 Alembic deprecation warnings。
- 前端 `npm run lint`（TypeScript）通过，`npm run build` 通过，Next.js 生产构建成功。
- `git diff --check` 通过；未提交、未推送。

### 下次预防

前端展示“支持实时事件”时必须检查是否实际连接 SSE；所有模型失败分支都要核对 AgentRun 的 `success/error`；自动化测试通过后仍需单独完成人工抖音安全验证和真实链路证据包。

## 2026-09-16｜opcweb 发布与产品文档复盘

- 项目：opcweb
- 类型：经验 / 验证 / 文档
- 关键词：GitHub、README、产品截图、服务启动、敏感信息
- 状态：已验证

### 背景与现象

需要把 AI Lead Radar 代码推送到 GitHub，并为 README 增加真实产品截图和完整功能说明。仓库同时包含源码、运行数据、环境变量和浏览器采集状态，直接提交存在误传风险。

### 根因

源码仓库与本地运行环境共用工作区；截图依赖 API 和 Web 服务运行，文档格式化工具也不一定存在于仓库根目录。

### 解决办法

- 推送前先执行 git fetch、git diff --check，查看暂存区文件和敏感模式。
- 只提交源码、文档和脱敏后的 docs/screenshots PNG；排除 .env、数据库、浏览器 Profile、.next 和日志。
- 服务停止导致 localhost 截图失败时，分别启动 API 8689 和 Web 5173，再用 CUA 检查页面，用 Playwright 保存真实截图。
- 评论池截图先模糊用户相关列；README 使用相对图片路径和真实能力说明。
- 根目录没有 Prettier 时，使用 npx --yes prettier --write README.md，不向项目写入额外依赖。

### 验证结果

后端此前通过 127 个测试，前端 TypeScript 检查和 Next.js 生产构建通过；README 的 5 张 PNG 已检查文件存在和 PNG 文件头；提交 ad2d5c2 已推送并通过 git ls-remote 验证远程 main。

### 下次预防

新项目必须先读三份全局文档并使用专属文件夹；截图前先检查端口和页面；发布前必须审查暂存区，而不是只看工作区状态。

### 可复用资产

- 根目录 AGENTS.md
- 根目录 全局工作台.md
- 根目录 新项目SOP.md
- opcweb 的 docs/screenshots/ 和 README.md

## 2026-09-17｜全量测试与本地运行态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 状态：自动化与本地 UI 冒烟已验证；真实抖音业务 E2E 仍需人工安全验证

### 验证内容

- 后端：`.venv\Scripts\python.exe -m pytest -q`
- 前端：`npm run lint`、`npm run build`
- 静态检查：`git diff --check`
- 运行态：API `/health`、`/ready`，Web 首页 HTTP 200
- 浏览器冒烟：真实本地 Chromium 加载首页并依次打开全部 15 个导航页面；无页面错误、无请求失败、无残留加载态

### 发现与解决

前端开发服务在执行 `npm run build` 后继续复用旧的 Next 开发态缓存，浏览器加载到的 `/_next` 资源出现 404，页面停留在“正在读取本地工作区”。重启 Web 开发服务后，静态资源恢复，首页可以读取真实项目并正常渲染全部导航页面。以后执行生产构建后，必须重启正在运行的 Next 开发服务再做浏览器验收。

### 结果

- 后端：`130 passed`，2 个 Alembic deprecation warnings，无失败。
- 前端 TypeScript 检查通过，Next.js production build 通过。
- `git diff --check` 通过。
- API 8689、Web 5173 仍在运行；本次未提交、未推送。
- 真实抖音关键词→视频→评论→入库及人工确认回复链路未用 Mock 冒充完成，仍待平台安全验证后的现场验收。

## 2026-09-17｜Windows 启动脚本编码兼容

- 项目：opcweb / AI Lead Radar
- 类型：踩坑 / 修复 / 验证
- 关键词：`start.ps1` `Windows PowerShell 5.1` `UTF-8 BOM` `启动`
- 状态：已验证

### 背景与现象

执行完整运行态验收时，`start.ps1` 在 Windows PowerShell 5.1 中因脚本文件无 UTF-8 BOM 被误解析，报告字符串未闭合；因此不能把脚本启动测试当作通过。

### 根因

脚本包含中文文本但以无 BOM UTF-8 保存，Windows PowerShell 5.1 按系统代码页读取，导致解析状态错乱。脚本中的 Python `-c` 调用和 API 子进程参数也改为更明确的变量/参数数组形式。

### 解决办法

为 `start.ps1` 保存 UTF-8 BOM，并使用 `$python` 变量执行密钥生成，将 API 命令先组装为 `$serverCommand` 后通过 `Start-Process -ArgumentList` 启动。

### 验证结果

Windows PowerShell 5.1 解析检查通过；`start.ps1` 可幂等识别已运行的 API 8689 与 Web 5173。运行态 `/health`、`/ready` 和首页均返回 200。

### 下次预防

Windows PowerShell 5.1 可执行的含中文脚本应保存为 UTF-8 BOM；启动脚本的解析检查与服务健康检查都纳入交付前验收。

## 2026-09-17｜本轮全部测试复核

- 项目：opcweb / AI Lead Radar
- 类型：验证
- 关键词：`pytest` `TypeScript` `Next.js build` `Playwright UI smoke` `DeepSeek`
- 状态：部分验证

### 背景与现象

用户要求执行全部测试并确认当前前后端运行状态。

### 根因

代码级测试与本地运行态可以独立验证；抖音真实搜索仍受平台安全验证页影响，登录状态不能替代搜索 E2E 成功。

### 解决办法

执行后端 pytest、前端 TypeScript 检查与生产构建；重启 Next 开发服务后，用本地 Playwright 依次点击 15 个导航页面；检查 API 健康探针、OpenAPI 路由、文本模型连接和启动脚本幂等性。

### 验证结果

- 后端：`130 passed`，仅 2 个 Alembic deprecation warnings。
- 前端：`npm run lint` 通过，`npm run build` 通过。
- UI：15 个导航页面均可切换，页面加载无卡住状态、无 console error、无 5xx 响应。
- 运行态：API `8689` 的 `/health`、`/ready` 和 Web `5173` 首页均返回 200；文本模型连接测试成功。
- 真实抖音 smoke：登录状态为 `LOGGED_IN`，搜索被 `DouyinVerificationRequired` 阻塞；未使用假数据或绕过平台验证。报告：`data/acceptance/douyin-playwright-smoke-20260917-all-tests.json`。

### 下次预防

交付结论必须区分自动化通过、本地运行通过与平台真实 E2E；只有人工完成抖音安全验证后，才能继续判定关键词到视频、评论入库和回复发送链路。

## 2026-09-17｜回复队列指标与 SSE 重放降噪

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 产品补强 / 验证
- 关键词：`Reply Queue` `SSE` `事件重放` `前后端对应`
- 状态：已验证（自动化）；真实抖音 E2E 仍待人工安全验证

### 背景与现象

功能矩阵要求 AI 回复页展示今日评论、AI 分析、潜客、待审核、今日回复、发送成功和失败等运营指标；任务页在 SSE 重放历史事件时会为每条事件立即触发一次任务与计划刷新，造成重复请求。

### 根因

回复页此前只加载回复列表，没有加载真实评论集合，因此缺少评论侧指标。SSE 历史事件是连续推送的，前端没有对事件触发的刷新进行合并。

### 解决办法

回复页并行读取当前项目的真实评论和回复记录，按持久化时间与真实状态计算 7 项指标；任务页将 SSE 事件刷新合并到 250ms 窗口，同时保留 5 秒持久化轮询兜底。

### 验证结果

`npm run lint`、`npm run build` 通过；本地 Playwright 验证回复页渲染 7 个指标、15 个导航页面无 console error/5xx；任务页 3.5 秒内仅产生 3 次任务请求，未再按历史事件逐条刷新。

### 下次预防

对可重放的实时事件流，UI 更新必须采用去重或节流，并以数据库快照为最终状态；指标必须来自真实 API 数据，不使用静态或随机数字。

## 2026-09-17｜文本模型配置边界加固

- 项目：opcweb / AI Lead Radar
- 类型：安全边界 / 修复 / 验证
- 关键词：`LLM_TEXT_ONLY_MODEL_REQUIRED` `DeepSeek` `vision` `vl`
- 状态：已验证

### 背景与现象

虽然代码没有视觉 Provider 或视觉依赖，但 OpenAI Compatible 的自由模型名如果直接放行，使用者仍可能误配视觉/多模态模型，与第一版纯文本约束不一致。

### 根因

此前只校验 Base URL、API Key 和 Model 是否非空，没有对常见视觉模型命名做边界校验。

### 解决办法

新增文本模型名称校验；包含 `vision`、`vl`、`multimodal`、`image`、`ocr` 或 `video` 的模型会被拒绝，API 返回 `LLM_TEXT_ONLY_MODEL_REQUIRED`，且不会发起模型请求。

### 验证结果

后端测试 `131 passed`；运行态使用 `DeepSeek-V4-Flash-Vision-Exp` 的文本连接测试返回 `ok=false` 与 `LLM_TEXT_ONLY_MODEL_REQUIRED`，未发起模型请求；原有文本模型配置保持不变，文本连接测试仍成功。

## 2026-09-17｜全部测试复核与真实抖音验收边界

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`pytest` `Next.js` `Playwright` `DOUYIN_VERIFICATION_REQUIRED` `crawler`
- 状态：自动化已通过，真实平台链路待人工验证

### 背景与现象

执行当前工作区的全部可重复验证，包括后端测试、前端类型检查与生产构建、本地 API/Web 运行态、桌面端 UI 导航、移动端布局、文本模型连接和两个真实抖音采集 smoke。

### 根因

代码质量和本地运行态没有失败；真实 Playwright 搜索在已登录状态下被抖音安全验证页拦截。外部 `douyin-comments-crawler` 的默认地址 `http://127.0.0.1:8000` 当前没有服务。独立 smoke 在 API 占用同一持久化 Profile 时会遇到浏览器锁，因此必须先停止本项目服务再运行独立 smoke。

### 解决办法

- 先执行 `.venv\\Scripts\\python.exe -m pytest -q`、`npm run lint`、`npm run build` 和 `git diff --check`。
- 重启本地服务后检查 `/health`、`/ready`、`/api/douyin/status` 和 `/api/settings/test-llm`。
- 用无头 Chromium 遍历全部 15 个导航页，并检查移动端菜单、横向溢出、控制台错误和 HTTP 失败。
- 独立 Playwright smoke 只记录真实的 `LOGGED_IN` 与安全验证错误，不使用 Mock、空结果或绕过平台验证；crawler smoke 只记录真实的 disconnected 状态。

### 验证结果

- 后端：`131 passed`，仅 2 个 Alembic deprecation warnings。
- 前端：`npm run lint` 通过，`npm run build` 通过。
- UI：桌面端 `15/15` 页面、回复页 `7/7` 指标、控制台错误 `0`、HTTP 失败 `0`；移动端无横向溢出，菜单可打开，控制台错误 `0`。
- 运行态：API `/health=200` 且 `running`，`/ready=200` 且 `ready`，Web `/=200`；Playwright 浏览器 `running`、登录态 `LOGGED_IN`；DeepSeek 文本连接成功。
- 真实 Playwright 独立 smoke：登录态 `LOGGED_IN`，搜索返回 `DOUYIN_VERIFICATION_REQUIRED`，报告为 `data\\acceptance\\douyin-playwright-smoke-20260917-all-tests-standalone.json`。
- 外部 crawler smoke：健康状态 `disconnected`，报告为 `data\\acceptance\\douyin-crawler-smoke-20260917-all-tests-rerun.json`。
- 因为尚未完成安全验证后的真实视频、评论 ID/文本、系统入库关联和人工复核，真实商业化 E2E 仍不能标记为通过。

### 下次预防

生产构建后必须重启 Next 开发服务再做浏览器验收；独立 Playwright smoke 不得与持久化 Profile 的 API 浏览器并发运行。真实平台验收必须把登录、搜索、评论、入库和回复 DOM 核验分成独立证据步骤。

### 可复用资产

- `scripts\\douyin_smoke_test.py`
- `scripts\\crawler_smoke_test.py`
- `docs\\real-acceptance-matrix.md`

## 2026-09-17｜前后端接口对应性复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 文档修正
- 关键词：`API contract` `Next.js` `FastAPI` `read-only smoke`
- 状态：已验证（真实平台依赖仍待人工）

### 背景与现象

继续核对前端页面实际调用的请求路径与 FastAPI 路由，避免页面看似可用但请求落到不存在的接口。

### 解决办法

从 `web/app/page.tsx` 提取 44 个请求路径，与后端 49 个路由进行动态参数归一化比对；任务的 `pause/resume/retry` 动态动作按后端三个独立路由核对。随后对当前数据库的实际项目、视频、评论、潜客和任务 ID 执行只读 API 冒烟。

### 验证结果

- 前端请求路径无未实现接口；仅通用任务动作路径需要按动作名映射到已有路由。
- 只读 API 冒烟 `26/26` 通过，未执行扫描、写入、回复或发送操作。
- 文档已同步当前真实状态，并移除二级评论稳定能力的过度承诺。
- Docker 验证未执行：当前 Windows 环境未安装 Docker CLI；不能把 Compose 生产启动标记为已验证。

### 下次预防

新增前端请求时同时增加后端路由契约测试；动态 URL 不能只按字符串字面比较，必须将参数和动作映射纳入检查。

## 2026-09-17｜生产源码边界门禁复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 安全边界
- 关键词：`MockProvider` `text-only` `git ls-files` `credentials`
- 状态：已验证

### 验证结果

对 Git 已跟踪的 65 个生产源码文件执行静态门禁：未发现 `MockProvider`、`MockLLMProvider`、`VisionProvider`、`ImageProvider` 或 `MultimodalProvider`；`.env` 与 `data/` 运行数据未被 Git 跟踪。该门禁不能替代真实抖音 E2E，但证明当前提交边界没有把这些实现或本地凭据纳入版本控制。

## 2026-09-17｜opcweb 文本模型边界修复提交

- 项目：opcweb / AI Lead Radar
- 类型：发布 / 验证
- 关键词：`git commit` `git push` `text-only` `GitHub`
- 状态：已验证

### 背景与现象

完成本轮纯文本 AI 边界、抖音安全验证识别、AgentRun 失败审计、前端状态展示和验收文档更新后，需要先提交并推送到 GitHub。

### 根因

工作区包含源码、测试、文档和全局工作台文件的待提交修改；本地运行数据、环境变量、浏览器 Profile、构建产物和日志不应进入远程仓库。

### 解决办法

- 先轮询后端测试与前端生产构建任务，确认通过。
- 仅暂存 Git 工作区中的源码、测试、文档和启动脚本；执行 `git diff --cached --check` 和敏感信息模式扫描。
- 创建提交 `d107322`，随后执行 `git push origin main`。

### 验证结果

- 提交成功：`d107322 Complete text-only AI lead radar validation`。
- 推送成功：远程 `github.com:skyconnfig/opcweb.git` 的 `main` 已从 `ad2d5c2` 更新到 `d107322`。
- 本次验收仍未把抖音安全验证、外部 crawler 和真实回复发送标记为通过。

### 下次预防

提交前固定检查暂存区文件、差异空白、凭据模式和远程分支；远程发布成功后再用 `git ls-remote origin main` 核对远程提交。

### 可复用资产

- `docs\\real-acceptance-matrix.md`
- `docs\\commercial-readiness.md`
- `start.ps1`

## 2026-09-17｜回复审核队列与前端批量操作修复

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 验证 / 发布
- 状态：已验证并推送

### 背景与现象

前一轮完成了评论分析事件契约、知识库不足时进入人工审核队列，以及前端评论来源展示、批量回复、关键词分类和潜客 B/C 级筛选；本轮需要把已验证修改提交到 GitHub。

### 解决办法

- 回复决策在需要人工审核但没有 AI 文本时保留为 `WAITING_REVIEW`，允许人工填写安全文本后再发送。
- 扫描流程补充 `comment.analyzed`、`lead.detected`、`reply.generated` 事件。
- 回复队列展示来源评论、用户和视频，支持单条编辑、批量选择和发送前确认。
- 评论池增加待回复筛选，关键词分类按真实数据动态展示，潜客池增加 B/C 级筛选。

### 验证结果

- 后端：`133 passed`，仅有 2 个 Alembic 配置弃用警告。
- 前端：`npm run lint` 通过。
- 前端：`npm run build` 通过。
- `git diff --cached --check` 通过，暂存区敏感信息模式扫描无命中。
- 提交：`7f5d23a Improve review queue and lead radar UX`。
- 远程 `origin/main` 已核对为 `7f5d23a68fd3c01955bc144ee44c7c97e83cb4ba`。
- 真实抖音安全验证、外部 crawler 连接和真实回复 E2E 仍未通过，不将其标记为完成。

### 下次预防

前端新增批量或状态操作时，同步核对后端请求体契约并增加真实接口或服务层测试；发布前继续执行后端测试、前端 lint/build、差异检查和远程提交核对。

## 2026-09-17｜自动回复人工确认与浏览器会话状态加固

- 项目：opcweb / AI Lead Radar
- 类型：安全边界 / 实现 / 验证
- 关键词：`HUMAN_REVIEW_REQUIRED` `COMMENT_ID_NOT_SENDABLE` `browser_sessions` `Playwright`
- 状态：已验证（代码与本地运行态）；真实抖音业务 E2E 仍待人工安全验证

### 背景与现象

审计发现扫描任务在启用项目自动回复策略且模型返回安全文本时，可能直接进入真实 Provider 发送路径；同时 DOM 缺少平台原生评论 ID 时会生成 fingerprint，不能将其作为真实回复目标。

### 根因

回复草稿生成和真实发送逻辑位于同一任务服务方法中，原有配置开关会触发自动发送。评论指纹适合去重和人工查看，但不具备可证明的平台注册身份。

### 解决办法

- 扫描任务统一只生成 `WAITING_REVIEW` 草稿并记录 `HUMAN_REVIEW_REQUIRED`，真实发送只保留人工审核/显式确认 API。
- Reply Policy 与 Douyin Playwright Provider 双重拒绝 `fingerprint`、`unknown` 等不可写评论身份，仅接受平台字段、DOM 稳定属性或真实评论 URL。
- 新增 `BrowserSessionManager` 命名边界和 `browser_sessions` 持久化表；只记录 Profile 路径和状态，不记录 Cookie、密码或 Token。
- 抖音状态接口向前端返回会话状态，账号页展示 `READY` / `LOGIN_REQUIRED` 等可审计状态。

### 验证结果

- 后端：`138 passed`，仅 2 个 Alembic 配置弃用警告。
- 前端：`npm run lint` 通过，`npm run build` 通过。
- `git diff --check` 通过。
- 本地运行态：API `8689` 的 `/health`、`/ready` 返回 200，Web `5173` 返回 200；真实持久化浏览器状态为 `LOGGED_IN`，会话记录为 `READY`。
- 未使用 Mock 替代真实采集，未绕过抖音安全验证；真实搜索/评论/回复 E2E 仍需人工完成平台验证后验收。

### 下次预防

任何会触发外部写操作的任务，都必须有人工确认门和稳定外部 ID 门；改动回复链路后同时检查任务服务、Provider、HTTP 接口和前端文案，避免“设置名称”与实际安全语义不一致。

### 可复用资产

- `backend\app\services\reply_policy.py`
- `backend\app\providers\douyin\browser_manager.py`
- `backend\alembic\versions\a7b8c9d0e1f2_add_browser_sessions.py`

## 2026-09-17｜手动同步接入统一获客链路与设置加载竞态修复

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 前后端契约 / 验证
- 关键词：`manual sync` `LeadJudgeAgent` `WAITING_REVIEW` `settings loading`
- 状态：已验证（自动化与本地运行态）；真实抖音数据仍待人工安全验证

### 背景与现象

前端视频详情中的“同步此视频评论”此前只把公开评论写入评论池，手动搜索与定时扫描的获客行为不一致；视频级扫描路由还会误启动整个项目扫描。设置页在异步读取持久化配置时允许保存，存在默认值覆盖风险。

### 解决办法

- 手动评论同步后复用 `RulePreFilter → LeadJudgeAgent → Lead/Reply 草稿` 文本链路，并返回分析统计与错误列表。
- `/api/videos/{video_id}/scan` 改为只处理指定视频，不再启动项目级扫描。
- 设置页在配置或回复策略加载完成前禁用编辑、测试和保存，并提供失败重试；使用版本号避免旧请求覆盖新项目状态。

### 验证结果

- 后端：`140 passed`，仅 2 个 Alembic 配置弃用警告。
- 前端：`npm run lint`、`npm run build` 通过。
- API `/health`、`/ready` 和 Web `5173` 均返回 200；抖音会话仍为 `LOGGED_IN / READY`。
- 新增回归测试证明手动同步可形成潜客判断，视频扫描路由不会扩大到其他关键词；未使用 Mock 冒充真实平台结果。

### 下次预防

每个写入公开数据的入口都必须说明是否仅同步、是否分析、是否生成草稿；前端新增接口调用时同时验证后端实际副作用范围。异步表单必须先完成真实配置读取，再允许覆盖持久化值。

## 2026-09-17｜前端静态资源二次现场复核

- 项目：opcweb / AI Lead Radar
- 类型：运行态复核 / 前端修复
- 状态：已验证

### 背景与现象

再次用浏览器访问时，首页 HTML 返回 200，但已运行的 Next Web 进程对其引用的 `/_next/static` CSS/JS 资源返回 404，表现为前端重新退化为裸 HTML。

### 解决办法

仅停止并重启当前仓库对应的 Web 进程，不触碰 API 和数据库；重启后等待 Next 首轮编译完成，再用首页实际返回的资源 URL 逐项探测。

### 验证结果

- 首页和 CSS 均返回 200，CSS 内容约 62KB。
- Playwright 依次打开 15 个前端导航页面，无 HTTP 失败、无 console error。
- `.product-shell` 计算样式为 `display: flex`，前端样式已恢复。
- `web` 目录 `npm run lint` 通过。

### 下次预防

前端运行态验收不能只检查 HTML；每次重启或生产构建后都要检查 HTML 引用的全部 Next 静态资源，并等待首轮编译完成后再进行页面验收。

### 追加复核

- 本轮 `npm run build` 通过；构建后故意复现开发服务静态资源失配，再由新版 `start.ps1` 检查全部 Next CSS/JS 资源并恢复 Web 服务。
- 恢复后再次打开 15 个导航页面：HTTP 失败 0、console error 0。

## 2026-09-17｜前端裸 HTML 现场恢复复核

- 项目：opcweb / AI Lead Radar
- 类型：运行态修复 / 验证
- 关键词：`Next.js` `CSS 404` `静态资源` `Playwright`
- 状态：已验证

### 背景与现象

用户提供的截图显示页面退化为浏览器默认样式，主要产品内容不可见。检查发现当前 Web 开发服务需要重启后才能重新绑定最新 `.next` 构建资源。

### 解决办法

只重启本项目 Web 开发服务，不修改 API、数据库或抖音登录 Profile；重启后用首页 HTML 引用的 CSS/JS URL 逐项探测，再进行桌面和移动端浏览器冒烟。

### 验证结果

- Web `5173` 返回 200；首页引用的 Next CSS/JS 资源全部返回 200。
- `.product-shell` 计算样式为 `display: flex`，首页不再显示裸 HTML。
- 桌面端 15 个导航页面：console error 0、request failure 0。
- 移动端菜单可打开，无横向溢出，console error 0。
- `web` 目录 `npm run lint` 与 `npm run build` 均通过；构建后已再次启动开发服务。

### 下次预防

执行 `npm run build` 后不要复用旧的 Next 开发进程；先重启 Web 服务，再检查首页实际引用的全部静态资源。

## 2026-09-17｜潜客 CRM 跟进功能前后端补齐

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 前后端契约 / 验证
- 关键词：`FollowTask` `follow_note` `LeadDrawer` `datetime-local`
- 状态：已验证（本地真实 API 与 UI）；真实抖音平台链路不因本项改变

### 背景与现象

后端已经提供潜客人工备注和跟进任务 API，但前端潜客详情抽屉没有对应的输入、创建、完成和取消操作，导致 CRM 闭环展示不完整。

### 解决办法

- 潜客抽屉接入 `GET/POST /api/leads/{lead_id}/follow-tasks` 和 `PATCH /api/follow-tasks/{follow_task_id}`。
- 增加人工备注保存，复用 `PATCH /api/leads/{lead_id}` 的 `follow_note` 字段。
- 增加任务内容、截止时间、待处理计数、完成/取消状态和错误反馈。
- 后端时间按 UTC 解释后在浏览器本地显示，避免 SQLite 无时区字符串被误当成本地时间。

### 验证结果

- 使用当前真实本地潜客数据打开详情抽屉，跟进任务接口返回 HTTP 200，相关控件可见。
- 桌面端无 console error 或请求失败；移动端布局验证通过。
- `npm run lint`、`npm run build` 通过；后端 `143 passed`。

### 下次预防

后端新增可操作 API 时，必须同时检查前端是否有真实入口、加载态、空态、错误态和成功状态；日期字段要明确时区再渲染和提交。

## 2026-09-17｜运行服务未加载最新后端源码

- 项目：opcweb / AI Lead Radar
- 类型：运行态一致性 / 修复 / 验证
- 状态：已验证

### 背景与现象

源码和迁移已包含 `follow_tasks`，但旧 API 进程的 OpenAPI 未暴露跟进任务路由；仅查看端口监听不能证明运行服务已经加载当前工作区。

### 解决办法

核对当前 Python 进程命令行后，仅重启本项目 API 进程；随后执行 `start.ps1`，并用 OpenAPI、健康探针和数据库表检查确认新源码已加载。

### 验证结果

- OpenAPI 已出现 `/api/leads/{lead_id}/follow-tasks` 和 `/api/follow-tasks/{follow_task_id}`。
- Alembic 当前为 `b8c9d0e1f2a3 (head)`，SQLite 已存在 `follow_tasks` 表。
- `/health` 返回 `running`，`/ready` 返回 `ready`。
- `/api/douyin/status` 返回 `browser=running`、`login=LOGGED_IN`、`browser_session_status=READY`。

### 下次预防

服务升级后必须同时核对进程命令行、OpenAPI 路由和迁移版本；端口仍在监听不代表新源码已经生效。

## 2026-09-17｜Provider 能力边界补到 API 层

- 项目：opcweb / AI Lead Radar
- 类型：后端契约 / 安全边界 / 测试
- 状态：已验证

### 背景与现象

前端会依据 Provider capabilities 禁用搜索、评论同步和扫描，但后端入口此前主要依赖具体 Provider 方法自行失败；未来切换到只支持部分能力的 Provider 时，API 可能在副作用前才暴露不一致错误。

### 解决办法

新增 `_require_provider_capability`，在真实视频搜索、公开评论同步、项目扫描三个 API 入口执行显式能力校验；错误统一返回 `PROVIDER_CAPABILITY_UNSUPPORTED`，并保留 Provider 与 capability 详情。未声明 capabilities 的内部旧适配器保持兼容，不改变生产 Provider 的显式能力约束。

### 验证结果

- 新增能力拒绝回归测试。
- 后端测试：`143 passed`。
- API 重启后 `/health=running`、`/ready=ready`，抖音登录态为 `LOGGED_IN`。
- 前端 15 页面仍可打开，无 console error 和 HTTP 失败。

### 下次预防

新增或切换 Provider 时，必须同时维护 capabilities、后端入口校验和前端禁用提示；不能只依赖页面按钮状态。

## 2026-09-17｜Docker 首次登录默认可见与验收数字同步

- 项目：opcweb / AI Lead Radar
- 类型：配置修复 / 文档一致性
- 状态：已验证（静态配置；当前机器未安装 Docker CLI）

### 解决办法

- `docker-compose.yml` 将 `DOUYIN_HEADLESS` 默认值从 `true` 调整为 `false`，首次运行可以打开可见 Chromium 供人工登录和平台验证；服务器环境仍可显式配置为 `true`。
- README 的后端测试数字从 `135 passed` 同步为当前实际 `143 passed`，保留真实抖音 E2E 尚未通过的限制说明。

### 验证结果

- `git diff --check` 通过。
- Docker 配置未执行容器级验证，因为当前环境没有 Docker CLI；不把 Compose 启动标记为已验证。

## 2026-09-17｜新增只读前端浏览器冒烟脚本

- 项目：opcweb / AI Lead Radar
- 类型：工具 / 验证
- 关键词：`Playwright` `Next.js` `移动端` `静态资源`
- 状态：已验证

### 背景与现象

首页 HTTP 200 不能证明前端已正常加载；Next 静态资源失配时仍可能返回 HTML，但浏览器只显示裸样式。

### 解决办法

新增 `scripts\frontend_smoke_test.py`，使用真实本地 Chromium 检查首页资源、产品壳层、15 个导航视图、桌面 console/request 错误、移动端菜单和横向溢出。脚本只读，不创建项目、不扫描、不分析评论、不发送回复；页面切换时主动关闭 SSE 造成的断开单独归类，不误报为业务请求失败。

### 验证结果

执行 `.venv\Scripts\python.exe scripts\frontend_smoke_test.py --report data\acceptance\frontend-smoke-20260917.json` 通过；15 个视图访问完成，产品壳层为 `flex`，console error 和非 SSE 请求失败均为 0，移动端菜单和无横向溢出检查通过。

### 下次预防

每次执行 Next 生产构建后，先运行 `start.ps1` 恢复开发服务，再执行该只读冒烟脚本；不要只看首页状态码。

## 2026-09-17｜前端修复交付前复核

- 项目：opcweb / AI Lead Radar
- 类型：前端修复 / 构建 / 运行态验证
- 关键词：`Next.js` `CSS 404` `hash 路由` `Playwright smoke`
- 状态：已验证

### 背景与现象

用户提供的截图显示首页退化为浏览器默认样式；此前修复已补齐静态资源失配恢复和 hash 深链接切换，本轮对当前运行态再次复核。

### 解决办法

- 通过 `start.ps1` 复用或恢复本项目 Web 服务，避免生产构建后的旧 `.next` 资源引用继续被使用。
- 检查首页实际引用的 CSS 资源状态及 `.product-shell` 样式内容。
- 使用只读前端冒烟脚本覆盖 15 个页面视图、15 个 hash 深链接、桌面请求/控制台错误、移动端菜单和横向溢出。

### 验证结果

- Web 首页 HTTP 200；CSS HTTP 200，包含 `.product-shell`。
- `npm run lint` 通过。
- `npm run build` 通过；随后重新确认 Web 服务可用。
- 前端冒烟通过：15 个视图、15 个 hash 视图，console errors 0，非 SSE 请求失败 0，移动端菜单与无横向溢出通过。
- API `8689` 健康检查仍为 `running`，未触碰数据库、Cookie 或抖音登录 Profile。

### 下次预防

前端修复不能只看首页 HTML 状态码；每次执行构建后都要先验证静态资源，再做桌面、深链接和移动端浏览器冒烟。

## 2026-09-17｜前端详情交互与可访问性复核

- 项目：opcweb / AI Lead Radar
- 类型：前端修复 / 可访问性 / 运行态验证
- 关键词：`role=dialog` `评论详情` `视频详情` `前端交互冒烟`
- 状态：已验证

### 背景与现象

前端主页面可以渲染，但评论和视频详情抽屉缺少明确的 dialog 语义；原有只读冒烟主要覆盖导航，未对当前真实工作区的详情面板逐一打开验证。

### 解决办法

- 为评论详情和视频详情抽屉补充 `role="dialog"`、`aria-modal="true"` 和稳定的中文 `aria-label`。
- 将详情面板、跟进任务入口和文本模型设置纳入 `scripts\frontend_smoke_test.py`；有真实行时打开并关闭面板，空工作区则明确记录 `skipped_empty`，不注入 fixture。
- 同步 README 和 UI 审查记录中的验证范围与测试数字。

### 验证结果

- `npm run lint` 通过。
- `npm run build` 通过。
- 后端 `.venv\Scripts\python.exe -m pytest -q`：145 passed，2 个 Alembic 弃用警告。
- 前端交互冒烟通过：15 个视图、15 个 hash 路由、评论/潜客/视频/任务详情、跟进任务入口和文本模型设置均通过；console error 0、非 SSE 请求失败 0；移动端菜单和横向溢出通过。

### 下次预防

前端页面验收不仅检查导航和静态资源，还要在存在真实业务记录时打开每个详情面板，确认后端读取、关闭操作、可访问性语义和错误边界均可用；空数据只能显示真实空态。

## 2026-09-18｜前端异常恢复与 Profile 跨进程互斥

- 项目：opcweb / AI Lead Radar
- 类型：前端可靠性 / 浏览器会话并发控制
- 状态：已验证

### 背景与现象

前端曾出现静态资源失配后退化为原生 HTML 的问题；独立审查还发现 Next.js 渲染异常没有专用恢复边界。同时，持久化 Chromium Profile 只有进程内 asyncio 锁，多个 API 进程可能同时打开同一 Profile。

### 解决办法

- 新增 `web/app/error.tsx` 和 `web/app/global-error.tsx`，为页面级和根布局级渲染错误提供不暴露内部细节的重新加载入口。
- `DouyinBrowserManager` 在启动持久化上下文前创建并持有 OS 级 Profile 锁；跨进程占用时返回 `PROFILE_LOCKED`，进程异常退出时由操作系统回收锁。
- 保留原有启动脚本的静态资源检查，不通过伪造数据掩盖前端或浏览器依赖失败。

### 验证结果

- `npm run lint` 通过。
- `npm run build` 通过，并实际生成 `error.js`、`global-error.js` 静态资源。
- 前端浏览器冒烟通过：15 个视图、15 个 hash 路由、详情面板、移动端菜单、无横向溢出，console errors 0，非 SSE 请求失败 0。
- 后端全量测试：`152 passed`。
- Profile 锁专项测试：`2 passed`。
- 修复分别提交并推送：`d8b7047`、`43357a7`。

### 下次预防

前端每次构建后必须检查 CSS/JS 实际状态并用真实浏览器冒烟；持久化 Chromium Profile 必须同时具备进程内互斥和跨进程 OS 锁，不能仅依赖单个 API worker 的内存锁。

## 2026-09-18｜项目级抖音 Profile 路由

- 项目：opcweb / AI Lead Radar
- 类型：浏览器会话隔离 / API 与前端契约
- 状态：已验证

### 背景与现象

原有抖音登录、搜索和评论同步入口都解析同一个全局 Provider；`browser_sessions.project_id` 虽已存在，但登录状态、启动关闭和采集请求没有把项目上下文传到 Provider，多个项目可能误用同一持久化 Profile。

### 解决办法

- 新项目创建时预先登记 `data/browser/projects/project-<id>` 独立 Profile，目录保持惰性创建，不复制或清除现有 Cookie。
- 对旧版本遗留的账号级 Profile，只由当前最晚更新的旧项目接管，以保持已有登录态；其他旧项目首次使用时走独立 Profile。
- 项目级抖音状态、登录、浏览器启动/关闭、视频搜索、评论同步、扫描 Worker、真实回复与回复核验统一通过项目 Provider 路由。
- 前端抖音账号页把当前项目 ID 传给状态和启动/关闭接口；非 Playwright 测试适配器仍走兼容路径。

### 验证结果

- 新增项目 Profile 路由测试：`2 passed`。
- 后端全量测试：`154 passed`，仅保留 2 个 Alembic 弃用警告。
- `npm run lint` 与 `npm run build` 通过。
- 重启 API/Web 后真实健康检查：`/health=running`、`/api/douyin/status?project_id=6` 返回 `login=LOGGED_IN`；项目 6 仍复用 `data/browser/douyin`，未触碰 Cookie。
- 前端浏览器冒烟再次通过：15 个视图、15 个 hash 路由、详情交互、移动端菜单、静态资源和错误边界检查均通过。

### 下次预防

新增需要持久化浏览器的项目时，必须在创建项目时登记 Profile 映射；任何抖音 API 不得重新回退到无项目上下文的全局 Provider。遗留 Profile 的归属规则必须保留可审计记录，不能为了隔离直接强制用户重新登录。

## 2026-09-18｜真实抖音搜索重试仍需人工平台验证

- 项目：opcweb / AI Lead Radar
- 类型：真实 E2E / 失败证据
- 状态：未通过，不能作为商业验收通过

### 背景与现象

API 重启后使用项目 6 的已登录持久化 Profile 重试关键词“长沙装修”。登录状态真实返回 `LOGGED_IN`，但搜索页在限定等待和一次重试后仍没有可解析的视频链接。

### 解决办法与边界

- 保留真实失败响应 `DOUYIN_PAGE_PARSE_FAILED`，不把空结果转换成成功，也没有写入伪造视频或评论。
- 保存脱敏 DOM 调试证据：搜索页标题为“发现更多精彩视频 - 抖音搜索”，未出现 `/video/` 结果链接，页面仍停留在搜索结果骨架/导航状态。
- 已在 Provider 中把可见安全验证标记单独归类为 `DOUYIN_VERIFICATION_REQUIRED`；本次页面没有可见验证标记，因此保持 `DOUYIN_PAGE_PARSE_FAILED`，避免凭猜测宣称被风控拦截。

### 验证结果

- `/health`：`running`，登录：`LOGGED_IN`。
- `POST /api/douyin/search`：HTTP 502，`DOUYIN_PAGE_PARSE_FAILED`；未产生新的真实视频/评论入库记录。
- 该结果证明失败边界可观测，但不证明“关键词 → 视频 → 评论”硬门槛通过。

### 下一步

需要用户在可见抖音浏览器中人工确认当前搜索页状态并完成平台要求的验证（不绕过验证码或风控），然后再次运行真实搜索和评论同步验收。若人工确认后仍无结果，再基于新的 DOM 证据校准 Selector 或页面动作。

### 回归审计

曾基于 `data-key="video"` DOM 增加点击视频标签的尝试，但真实重试显示点击后重试阶段丢失搜索输入框；该动作没有足够证据证明能恢复结果，已立即回滚提交 `1a7877d`。后续不再把“页面存在视频标签”推断为“点击它一定能触发可用搜索结果”。

## 2026-09-18｜抖音人工验证可恢复任务与 Next 静态资源回归

- 项目：opcweb / AI Lead Radar
- 类型：采集任务状态机 / 前端开发运行时
- 状态：已验证（真实平台验证动作本身仍需人工触发）

### 背景与根因

抖音 Playwright Provider 已能识别可见验证页，但扫描服务把 `DouyinVerificationRequired` 当作普通异常，任务落到 `failed`，浏览器虽可能仍在运行，前端没有稳定的人工验证等待态。此前 Web 开发服务器重启后，浏览器还请求旧的 `layout.css` 地址，日志出现一次 404，表现为裸 HTML。

### 解决办法

- 任务遇到真实验证页时持久化为 `verification_required`，不设置 `finished_at`，不关闭或重启 Chrome，不推进当前 keyword/video/comment checkpoint。
- Worker 每轮检测等待中的项目；当同一持久化浏览器的登录状态恢复为 `LOGGED_IN` 时，将任务重新排队并发布 `task.verification_resolved`，随后由现有 Worker 从 checkpoint 继续。
- 任务中心显示“抖音需要人工验证”，明确要求保持 Chrome 打开并手工完成拖拽；验证态下账号页主按钮只检查状态，不重新导航浏览器。
- Next 开发配置显式允许 `127.0.0.1` 与 `localhost`，并在启动脚本的静态资源检查后重启失配的 Web 进程。

### 验证结果

- `pytest -q`：157 passed；新增验证等待与自动恢复测试通过。
- `npm run lint`：通过。
- `npm run build`：通过。
- 前端真实 Playwright 冒烟：15 个视图、15 个 hash 路由、交互检查、移动端无横向溢出、console errors 0、非 SSE 请求失败 0。
- 重启后 8 个 Next 静态资源均 HTTP 200；`/health` 返回 running，抖音登录状态返回 `LOGGED_IN`。

### 限制

本次自动恢复逻辑已通过测试，但没有伪造或强行触发抖音验证码；真实验证页面出现时仍需用户在可见 Chrome 中人工完成平台验证。真实搜索结果硬门槛仍以平台页面实际 DOM 为准。

## 2026-09-18｜项目工作区管理闭环

- 项目：opcweb / AI Lead Radar
- 类型：项目管理 / 前端工作区交互 / 数据清理
- 状态：已验证

### 背景与需求

工作区侧栏原来只能切换项目，不能在当前界面新增、删除、重命名或查找项目；后端也没有项目更新和删除接口。

### 解决办法

- 新增 PATCH /api/projects/{project_id}，仅允许安全修改项目名称，并拒绝空白名称。
- 新增 DELETE /api/projects/{project_id}，按外键依赖顺序清理项目下的关键词、视频、评论、回复、潜客、跟进任务、扫描任务、任务产物、Agent 运行记录和浏览器会话元数据，再删除项目本身；不删除磁盘上的浏览器 Profile，避免误删登录态文件。
- 工作区切换器增加查找、新建、重命名、二次确认删除、删除当前项目后切换剩余项目；新建项目进入空白的业务画像表单，不复制当前项目内容。
- 保留真实数据边界，没有添加 Mock 数据、视觉模型或自动回复行为。

### 验证结果

- 后端全量测试：158 passed，2 个 Alembic 弃用警告。
- 前端 npm run lint：通过。
- 前端 npm run build：通过。
- 浏览器冒烟：15 个视图、15 个 hash 路由、移动端无横向溢出、console errors 0、非 SSE 请求失败 0。
- 工作区菜单专项冒烟：查找无匹配项目、恢复项目列表、Escape 关闭均通过。
- API /health：running；当前数据库项目数：6。

### 下次预防

涉及项目删除时必须覆盖所有带 project_id 的业务表及任务/潜客关联表，并用专门测试证明其他项目仍保留；前端删除必须二次确认，删除当前项目后不能让视图继续请求已删除的项目 ID。

## 2026-09-18｜项目工作区 CRUD 运行态验收与 API 重启

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 前端交互 / 运行态
- 关键词：`项目工作区` `新增` `重命名` `删除` `查找` `OpenAPI` `API 重启`
- 状态：已验证

### 背景与现象

工作区菜单已支持新增、查找、重命名和删除，但前端构建通过不等于当前运行中的 API 进程已经加载最新路由。现场检查发现旧 API 进程的 OpenAPI 只暴露项目 GET，未暴露 PATCH/DELETE。

### 根因

`start.ps1` 为避免不必要重启，会复用健康的 API 进程；源码更新后旧进程不会自动加载新路由。前端功能因此可能在代码层存在、但运行态仍不可用。

### 解决办法

- 停止并重新启动本项目 API/Web 进程，不触碰数据库和抖音 Profile。
- 用 `/openapi.json` 确认 `/api/projects/{project_id}` 同时暴露 GET、PATCH、DELETE。
- 用真实本地 API 和浏览器完成一次临时项目的新增、查找、重命名、二次确认删除流程，并在结束后确认临时数据已清理。
- 总览页的抖音状态请求补充当前 `project_id`，避免切换工作区后读取错误项目的状态。

### 验证结果

- 后端：`158 passed`，仅 2 个 Alembic 弃用警告。
- 前端：`npm run lint` 通过；`npm run build` 通过。
- 浏览器冒烟通过：15 个视图、15 个 hash 路由、详情交互、移动端菜单和静态资源检查通过。
- 工作区 CRUD 浏览器验收通过：新增临时项目、查找、重命名、删除；API 最终确认临时项目返回 404。
- 运行态：API `8689`、Web `5173`；健康检查返回 `running`；工作区项目数恢复为 6。
- 提交并推送：`db57256 fix: scope dashboard douyin status to project`。

### 下次预防

涉及 API 路由新增或修改时，不能只运行启动脚本并看到端口存活；必须检查 OpenAPI 路由和至少一条真实请求。构建后仍需执行前端浏览器冒烟，确认 HTML 与静态资源匹配。

## 2026-09-18｜手动评论同步分页闭环

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 前后端契约 / 验证
- 关键词：`CommentScanResult` `next_cursor` `all_pages` `公开评论` `文本潜客判断`
- 状态：已验证（真实抖音平台分页仍需人工安全验证后的现场验收）

### 背景与现象

视频详情中的“同步此视频评论”此前只调用一次 Provider `get_comments()`，即使 Provider 返回 `has_more=true` 和 `next_cursor`，前端也不会继续请求，无法证明关键词→视频→评论链路读取完整分页。

### 根因

后端同步接口只处理单页并直接进入文本分析；前端同步动作没有消费分页游标，导致真实 DOM 或外部 Provider 的后续公开评论停留在未同步状态。

### 解决办法

- 后端同步接口新增 `all_pages` 参数，返回 `pages`、累计 `received`、`next_cursor` 和 `has_more`。
- `all_pages=true` 时按游标继续读取，检测游标不前进并返回可诊断错误；每页先提交真实评论，最多自动处理 100 页，避免异常 Provider 无限循环。
- 视频级扫描调用完整分页模式；前端手动同步按 `next_cursor` 逐页请求，汇总新增、更新和读取数量后刷新详情。
- 覆盖状态使用最不乐观的聚合结果，不把部分采集宣称为完整；没有加入 Mock 评论或视觉模型。

### 验证结果

- 新增分页测试验证两页游标、累计数量、覆盖状态和文本分析输入；后端全量：`159 passed`，仅 2 个 Alembic 弃用警告。
- 前端 `npm run lint`、`npm run build` 通过。
- 前端浏览器冒烟通过：15 个视图、15 个 hash 路由、详情交互、移动端菜单、无控制台错误和非 SSE 请求失败。
- 重启 API/Web 后 OpenAPI 暴露 `limit`、`cursor`、`all_pages`；健康检查返回 `running`。
- 提交并推送：`54d4c58 feat: paginate manual douyin comment sync`。

### 当前限制

本次代码和测试验证了分页协议与系统入库逻辑，尚未替代抖音平台的真实登录、安全验证和现场 DOM 验收；真实平台硬门槛仍按 `docs/real-acceptance-matrix.md` 执行。

## 2026-09-18｜知识库变更的项目隔离

- 项目：opcweb / AI Lead Radar
- 类型：安全 / 前后端契约 / 验证
- 关键词：`KnowledgeEntry` `project_id` `知识库` `多项目隔离`
- 状态：已验证

### 背景与现象

知识库列表和新增接口按项目工作区过滤，但更新、启用/停用和删除接口此前只接收全局条目 ID。知道其他项目的条目 ID 时，后端可能误修改或删除不属于当前项目的文本知识。

### 根因

更新和删除接口没有把当前项目作为请求边界，也没有校验 `KnowledgeEntry.project_id` 与当前工作区一致；前端虽然从当前项目列表发起操作，但不能替代后端授权边界。

### 解决办法

- `PUT /api/knowledge/{knowledge_id}` 和 `DELETE /api/knowledge/{knowledge_id}` 要求 `project_id` 查询参数。
- 后端在写入或删除前校验条目归属，不匹配统一返回 404，避免泄露跨项目条目存在性。
- 知识库当前前端和兼容视图的更新、启用/停用、删除请求均传递当前项目 ID。
- 新增测试覆盖跨项目更新/删除拒绝、同项目更新和同项目删除。

### 验证结果

- 后端全量：`160 passed`，仅 2 个 Alembic 弃用警告。
- 前端 `npm run lint`、`npm run build` 通过。
- 浏览器冒烟通过：15 个视图、详情交互、移动端菜单、静态资源和请求错误检查通过。
- 重启 API 后 OpenAPI 确认知识库 PUT/DELETE 均包含 `project_id`；健康检查返回 `running`。
- 提交并推送：`e58cdb0 fix: enforce project scope for knowledge mutations`。

### 下次预防

所有带 `project_id` 的业务资源，更新、删除和读取详情都必须在后端完成归属校验；不能把前端当前项目状态当作权限控制。
