# 全局复利与踩坑日志

## 文档作用

记录跨项目可复用的经验、失败原因、修复方案、验证证据和预防措施，让后续项目可以复用已经验证过的路径，减少重复踩坑。

## 记录原则

- 及时记录：问题解决后尽快补充，不依赖事后回忆。
- 事实优先：区分已验证结果、推测和待验证事项。
- 可复用：重点记录触发条件、具体做法和验证方式。
- 可检索：每条记录包含项目名、日期和关键词。
- 不记录密码、Token、私钥、完整连接串等敏感信息；使用占位符代替。

## 条目模板

复制以下模板新增日志条目，按时间倒序排列：

```markdown
## YYYY-MM-DD｜简短标题

- 项目：项目名
- 类型：经验 / 踩坑 / 决策 / 工具 / 验证
- 关键词：`keyword-a` `keyword-b`
- 状态：已验证 / 部分验证 / 待验证 / 已失效

### 背景与现象

描述当时要完成什么，以及实际发生了什么。

### 根因

记录已确认的原因；如果只是推测，明确标注“推测”。

### 解决办法

记录可以复用的命令、步骤、代码位置或判断方法。敏感值使用占位符。

### 验证结果

记录执行了什么验证、结果是什么、证据在哪里。

### 下次预防

说明以后如何更早发现或避免同类问题。

### 可复用资产

列出脚本、模板、文档或其他可复用文件的路径。
```

## 推荐记录范围

- 环境安装、版本兼容和启动方式
- 构建、测试、部署和回滚中的失败与修复
- 用户明确确认过的产品决策和目录约定
- 可复用的脚本、检查清单和验证命令
- 发现的限制、未解决问题和后续替代方案

## 不应记录的内容

- 未验证的猜测作为确定结论
- 密码、API Key、Cookie、私钥和真实生产凭据
- 与项目无关的长篇过程转录
- 没有复现条件、解决步骤或验证结果的空泛感想

## 2026-09-17｜opcweb 当前状态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`pytest` `Next.js` `端口` `真实验收` `纯文本 AI`
- 状态：部分验证

### 背景与现象

复核当前仓库能否继续开发和本地试用。代码仓库没有运行中的 API 或 Web 进程，历史 SQLite 数据仍保留，真实采集评论的覆盖状态均为 `partial`。

### 根因

当前只完成了代码级验证和此前保存的真实 smoke 证据；本次复核没有启动服务，也没有重新执行抖音登录、搜索和评论采集，因此不能把历史证据升级为当前运行验收。

### 解决办法

- 先用 `git status --short --branch`、端口检查和 SQLite 只读统计确认现场。
- 后端使用 `.venv\Scripts\python.exe -m pytest -q`；前端在 `web` 目录执行 `npm run lint` 和 `npm run build`。
- 真实试用前按 `docs\real-acceptance-matrix.md` 补齐系统入库关联、人工复核和失败上下文，不把 `partial` 当作完整覆盖。

### 验证结果

- 后端：127 passed，2 个 Alembic deprecation warnings。
- 前端：TypeScript lint 通过，Next.js production build 通过。
- `git diff --check` 通过；API 8689 和 Web 5173 在本次检查时均未监听。
- 本地数据库只读统计：4 个项目、106 个关键词、26 个视频、259 条评论、27 个潜客、262 次 Agent 运行；259 条评论覆盖均为 `partial`。

### 下次预防

代码检查通过后仍要单独执行 `start.ps1`、`/health`、`/ready`、首页和真实 Provider smoke；上线判断以真实证据包为准，而不是仅以测试、构建或截图为准。

### 可复用资产

- `docs\commercial-readiness.md`
- `docs\real-acceptance-matrix.md`
- `start.ps1`

## 2026-09-17｜本地服务与真实登录复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 决策
- 关键词：`start.ps1` `health` `Playwright` `LOGGED_IN` `Next.js`
- 状态：部分验证

### 背景与现象

为完成前后端对应关系复核，启动本地 API 与 Web，并检查页面、接口和持久化抖音登录状态。

### 根因

数据库中的历史 `content_provider` 设置指向 `Douyin Comments Crawler`，导致启动后默认 Provider 为 disconnected；这不是代码自动降级，而是本地持久化配置覆盖了 `.env` 默认值。

### 解决办法

- 通过 Provider 激活接口将本地当前源切换为 `Douyin Playwright`，不改生产代码。
- 用 `/ready`、`/health`、`/api/douyin/status` 和首页 HTTP 响应检查运行态。
- 逐项打开前端 14 个导航页面，检查页面标题和浏览器控制台错误。

### 验证结果

- API `8689`、Web `5173` 正常监听；`/ready` 成功，`/health` 为 `running`。
- `/api/douyin/status` 返回 `browser=running`、`login=LOGGED_IN`；持久化 Profile 被正确恢复。
- 首页 HTTP 200；14 个导航页面均能渲染，前端控制台错误为 0。
- 当前 `Douyin Comments Crawler` 仍为 disconnected，未伪造或降级为样例数据。

### 下次预防

启动后优先检查数据库持久化 Provider 是否覆盖 `.env`；真实验收必须同时记录当前 Provider、登录状态、任务 ID、真实视频/评论和人工复核结果。

### 可复用资产

- `start.ps1`
- `backend\app\main.py`
- `web\app\page.tsx`
- `docs\real-acceptance-matrix.md`

## 2026-09-17｜真实搜索复核遇到平台安全验证

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`douyin/search` `DOUYIN_VERIFICATION_REQUIRED` `DeepSeek` `E2E`
- 状态：待人工处理

### 背景与现象

在服务已启动、Provider 已切换为 Playwright、登录状态显示 `LOGGED_IN` 后，使用项目已有关键词调用真实 `/api/douyin/search`。

### 根因

抖音搜索页面返回需要人工完成安全验证，API 返回 `409 DOUYIN_VERIFICATION_REQUIRED`。登录态成功不代表当前搜索动作已通过平台验证；不能通过代码绕过验证码或风控。

### 解决办法

- 保留真实错误，不降级到 Mock、Demo 或硬编码结果。
- 在已打开的 Playwright 抖音浏览器中由用户人工完成平台要求的验证，完成后重新运行搜索与评论 smoke。
- 同时调用 `/api/settings/test-llm` 验证文本模型链路。

### 验证结果

- DeepSeek 文本连接测试成功，使用已保存的文本模型配置；未调用视觉模型。
- 真实抖音搜索被安全验证拦截，当前 E2E 仍不能判定为本次运行通过。

### 下次预防

把 `LOGGED_IN`、搜索成功、视频 DOM 结果和评论 ID/文本分别作为独立验收步骤；任何一步出现安全验证都暂停并交给用户处理。

### 可复用资产

- `scripts\douyin_smoke_test.py`
- `docs\real-acceptance-matrix.md`
- `backend\app\providers\douyin\playwright_provider.py`

## 2026-09-17｜安全验证页错误分类修复

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 验证
- 关键词：`DouyinVerificationRequired` `page_title` `selector drift`
- 状态：已验证

### 背景与现象

真实搜索在抖音“验证码中间页”上曾被报告为 `DOUYIN_SELECTOR_NOT_FOUND`，前端无法明确提示用户需要人工完成验证。

### 根因

安全验证页可能没有可读的 body 文本或可见验证 selector，但页面标题仍是明确的“验证码中间页”；旧逻辑只检查 DOM selector，漏掉了标题/URL 这一文本元数据证据。

### 解决办法

- 新增基于页面标题和 URL 的安全验证页识别。
- `_detect_login_status`、必需 selector 和集合 selector 均统一抛出 `DouyinVerificationRequired`。
- 未读取截图、Canvas 或挑战几何，也未绕过任何平台验证。
- 先补 2 个回归测试，再修改 Provider。

### 验证结果

- 定向测试：2 passed。
- 全量后端测试：129 passed，2 个 Alembic deprecation warnings。
- 重启服务后真实搜索现在稳定返回 `409 DOUYIN_VERIFICATION_REQUIRED`，错误分类正确。
- `/health` 仍为 `running`，登录状态仍为 `LOGGED_IN`，DeepSeek 文本连接仍成功。

### 下次预防

平台页面异常需区分登录、人工验证、页面加载失败和 selector 漂移；先记录真实页面 URL/标题，再决定错误类型，禁止用空结果或 Mock 掩盖。

### 可复用资产

- `backend\app\providers\douyin\playwright_provider.py`
- `tests\test_douyin_playwright_contract.py`

## 2026-09-17｜当前项目运行态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 状态复核
- 关键词：`health` `ready` `Next.js` `pytest` `Git` `商业化`
- 状态：部分验证

### 背景与现象

检查当前工作区、服务运行态、测试结果和远程同步状态，确认是否可以继续本地试用。

### 根因

当前代码级质量门槛已通过，但抖音真实搜索此前被平台安全验证页拦截；登录状态为 `LOGGED_IN` 不等于搜索链路已完成真实 E2E。商业化能力仍停留在单租户、小规模、人工确认跟进边界。

### 解决办法

- 只读检查三份全局文档、Git 工作区、监听端口、健康探针和 OpenAPI 路由。
- 用 `.venv\Scripts\python.exe -m pytest -q` 复核后端测试。
- 将 `/api\keywords` 误判为缺失接口的检查改为使用实际项目级路由 `/api\projects\{project_id\}/keywords`；不要把不存在的汇总路由当作功能失败。

### 验证结果

- API `8689`、Web `5173` 正在监听；`/health` 返回 `running`，`/ready` 通过。
- 抖音 Playwright 浏览器正在运行，持久化登录状态为 `LOGGED_IN`；DeepSeek 文本模型已配置。
- 后端测试 `129 passed`，仅有 2 个 Alembic deprecation warnings；Web 首页 HTTP 200。
- 当前分支 `main` 与 `origin/main` 同步，但工作区仍有 2 个代码文件修改和 4 个全局文档未跟踪，尚未提交/推送。

### 下次预防

本地可运行、接口可访问和代码测试通过，只能证明开发态健康；再次判断商业化前必须重新完成人工安全验证后的真实“关键词 → 视频 → 评论 → 入库 → 规则预筛 → 文本 LLM → 潜客池”链路，并补齐脱敏证据包。

## 2026-09-17｜前后端对应性补强与全量自动化验证

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 验证
- 关键词：`SSE` `AgentRun` `评论筛选` `抖音账号状态` `Next.js build`
- 状态：已验证（自动化）；真实抖音 E2E 仍待人工安全验证

### 背景与现象

功能矩阵复核发现任务中心虽展示“数据库 + SSE”，前端实际只有轮询；评论池缺少未分析、B 级和失败筛选；直接评论分析或回复生成失败且没有有效模型调用记录时，审计记录存在被默认标成成功的风险。

### 根因

前端尚未消费 `/api/events/stream`；失败记录辅助函数在没有 `last_call` 时默认 `success=True`；账号状态接口返回字段不足以完整展示账号和登录时间。

### 解决办法

- 任务中心接入可恢复 SSE，并保留轮询作为断线兜底。
- AgentRun 记录支持显式 `success/error`，直接 Agent 失败路径明确写入失败。
- 评论池补齐状态筛选；抖音状态接口补充账号展示所需的非敏感字段。

### 验证结果

- 后端全量测试：`130 passed`，2 个 Alembic deprecation warnings。
- 前端 `npm run lint`（TypeScript）通过，`npm run build` 通过，Next.js 生产构建成功。
- `git diff --check` 通过；未提交、未推送。

### 下次预防

前端展示“支持实时事件”时必须检查是否实际连接 SSE；所有模型失败分支都要核对 AgentRun 的 `success/error`；自动化测试通过后仍需单独完成人工抖音安全验证和真实链路证据包。

## 2026-09-16｜opcweb 发布与产品文档复盘

- 项目：opcweb
- 类型：经验 / 验证 / 文档
- 关键词：GitHub、README、产品截图、服务启动、敏感信息
- 状态：已验证

### 背景与现象

需要把 AI Lead Radar 代码推送到 GitHub，并为 README 增加真实产品截图和完整功能说明。仓库同时包含源码、运行数据、环境变量和浏览器采集状态，直接提交存在误传风险。

### 根因

源码仓库与本地运行环境共用工作区；截图依赖 API 和 Web 服务运行，文档格式化工具也不一定存在于仓库根目录。

### 解决办法

- 推送前先执行 git fetch、git diff --check，查看暂存区文件和敏感模式。
- 只提交源码、文档和脱敏后的 docs/screenshots PNG；排除 .env、数据库、浏览器 Profile、.next 和日志。
- 服务停止导致 localhost 截图失败时，分别启动 API 8689 和 Web 5173，再用 CUA 检查页面，用 Playwright 保存真实截图。
- 评论池截图先模糊用户相关列；README 使用相对图片路径和真实能力说明。
- 根目录没有 Prettier 时，使用 npx --yes prettier --write README.md，不向项目写入额外依赖。

### 验证结果

后端此前通过 127 个测试，前端 TypeScript 检查和 Next.js 生产构建通过；README 的 5 张 PNG 已检查文件存在和 PNG 文件头；提交 ad2d5c2 已推送并通过 git ls-remote 验证远程 main。

### 下次预防

新项目必须先读三份全局文档并使用专属文件夹；截图前先检查端口和页面；发布前必须审查暂存区，而不是只看工作区状态。

### 可复用资产

- 根目录 AGENTS.md
- 根目录 全局工作台.md
- 根目录 新项目SOP.md
- opcweb 的 docs/screenshots/ 和 README.md

## 2026-09-17｜全量测试与本地运行态复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 状态：自动化与本地 UI 冒烟已验证；真实抖音业务 E2E 仍需人工安全验证

### 验证内容

- 后端：`.venv\Scripts\python.exe -m pytest -q`
- 前端：`npm run lint`、`npm run build`
- 静态检查：`git diff --check`
- 运行态：API `/health`、`/ready`，Web 首页 HTTP 200
- 浏览器冒烟：真实本地 Chromium 加载首页并依次打开全部 15 个导航页面；无页面错误、无请求失败、无残留加载态

### 发现与解决

前端开发服务在执行 `npm run build` 后继续复用旧的 Next 开发态缓存，浏览器加载到的 `/_next` 资源出现 404，页面停留在“正在读取本地工作区”。重启 Web 开发服务后，静态资源恢复，首页可以读取真实项目并正常渲染全部导航页面。以后执行生产构建后，必须重启正在运行的 Next 开发服务再做浏览器验收。

### 结果

- 后端：`130 passed`，2 个 Alembic deprecation warnings，无失败。
- 前端 TypeScript 检查通过，Next.js production build 通过。
- `git diff --check` 通过。
- API 8689、Web 5173 仍在运行；本次未提交、未推送。
- 真实抖音关键词→视频→评论→入库及人工确认回复链路未用 Mock 冒充完成，仍待平台安全验证后的现场验收。

## 2026-09-17｜Windows 启动脚本编码兼容

- 项目：opcweb / AI Lead Radar
- 类型：踩坑 / 修复 / 验证
- 关键词：`start.ps1` `Windows PowerShell 5.1` `UTF-8 BOM` `启动`
- 状态：已验证

### 背景与现象

执行完整运行态验收时，`start.ps1` 在 Windows PowerShell 5.1 中因脚本文件无 UTF-8 BOM 被误解析，报告字符串未闭合；因此不能把脚本启动测试当作通过。

### 根因

脚本包含中文文本但以无 BOM UTF-8 保存，Windows PowerShell 5.1 按系统代码页读取，导致解析状态错乱。脚本中的 Python `-c` 调用和 API 子进程参数也改为更明确的变量/参数数组形式。

### 解决办法

为 `start.ps1` 保存 UTF-8 BOM，并使用 `$python` 变量执行密钥生成，将 API 命令先组装为 `$serverCommand` 后通过 `Start-Process -ArgumentList` 启动。

### 验证结果

Windows PowerShell 5.1 解析检查通过；`start.ps1` 可幂等识别已运行的 API 8689 与 Web 5173。运行态 `/health`、`/ready` 和首页均返回 200。

### 下次预防

Windows PowerShell 5.1 可执行的含中文脚本应保存为 UTF-8 BOM；启动脚本的解析检查与服务健康检查都纳入交付前验收。

## 2026-09-17｜本轮全部测试复核

- 项目：opcweb / AI Lead Radar
- 类型：验证
- 关键词：`pytest` `TypeScript` `Next.js build` `Playwright UI smoke` `DeepSeek`
- 状态：部分验证

### 背景与现象

用户要求执行全部测试并确认当前前后端运行状态。

### 根因

代码级测试与本地运行态可以独立验证；抖音真实搜索仍受平台安全验证页影响，登录状态不能替代搜索 E2E 成功。

### 解决办法

执行后端 pytest、前端 TypeScript 检查与生产构建；重启 Next 开发服务后，用本地 Playwright 依次点击 15 个导航页面；检查 API 健康探针、OpenAPI 路由、文本模型连接和启动脚本幂等性。

### 验证结果

- 后端：`130 passed`，仅 2 个 Alembic deprecation warnings。
- 前端：`npm run lint` 通过，`npm run build` 通过。
- UI：15 个导航页面均可切换，页面加载无卡住状态、无 console error、无 5xx 响应。
- 运行态：API `8689` 的 `/health`、`/ready` 和 Web `5173` 首页均返回 200；文本模型连接测试成功。
- 真实抖音 smoke：登录状态为 `LOGGED_IN`，搜索被 `DouyinVerificationRequired` 阻塞；未使用假数据或绕过平台验证。报告：`data/acceptance/douyin-playwright-smoke-20260917-all-tests.json`。

### 下次预防

交付结论必须区分自动化通过、本地运行通过与平台真实 E2E；只有人工完成抖音安全验证后，才能继续判定关键词到视频、评论入库和回复发送链路。

## 2026-09-17｜回复队列指标与 SSE 重放降噪

- 项目：opcweb / AI Lead Radar
- 类型：修复 / 产品补强 / 验证
- 关键词：`Reply Queue` `SSE` `事件重放` `前后端对应`
- 状态：已验证（自动化）；真实抖音 E2E 仍待人工安全验证

### 背景与现象

功能矩阵要求 AI 回复页展示今日评论、AI 分析、潜客、待审核、今日回复、发送成功和失败等运营指标；任务页在 SSE 重放历史事件时会为每条事件立即触发一次任务与计划刷新，造成重复请求。

### 根因

回复页此前只加载回复列表，没有加载真实评论集合，因此缺少评论侧指标。SSE 历史事件是连续推送的，前端没有对事件触发的刷新进行合并。

### 解决办法

回复页并行读取当前项目的真实评论和回复记录，按持久化时间与真实状态计算 7 项指标；任务页将 SSE 事件刷新合并到 250ms 窗口，同时保留 5 秒持久化轮询兜底。

### 验证结果

`npm run lint`、`npm run build` 通过；本地 Playwright 验证回复页渲染 7 个指标、15 个导航页面无 console error/5xx；任务页 3.5 秒内仅产生 3 次任务请求，未再按历史事件逐条刷新。

### 下次预防

对可重放的实时事件流，UI 更新必须采用去重或节流，并以数据库快照为最终状态；指标必须来自真实 API 数据，不使用静态或随机数字。

## 2026-09-17｜文本模型配置边界加固

- 项目：opcweb / AI Lead Radar
- 类型：安全边界 / 修复 / 验证
- 关键词：`LLM_TEXT_ONLY_MODEL_REQUIRED` `DeepSeek` `vision` `vl`
- 状态：已验证

### 背景与现象

虽然代码没有视觉 Provider 或视觉依赖，但 OpenAI Compatible 的自由模型名如果直接放行，使用者仍可能误配视觉/多模态模型，与第一版纯文本约束不一致。

### 根因

此前只校验 Base URL、API Key 和 Model 是否非空，没有对常见视觉模型命名做边界校验。

### 解决办法

新增文本模型名称校验；包含 `vision`、`vl`、`multimodal`、`image`、`ocr` 或 `video` 的模型会被拒绝，API 返回 `LLM_TEXT_ONLY_MODEL_REQUIRED`，且不会发起模型请求。

### 验证结果

后端测试 `131 passed`；运行态使用 `DeepSeek-V4-Flash-Vision-Exp` 的文本连接测试返回 `ok=false` 与 `LLM_TEXT_ONLY_MODEL_REQUIRED`，未发起模型请求；原有文本模型配置保持不变，文本连接测试仍成功。

## 2026-09-17｜全部测试复核与真实抖音验收边界

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 踩坑
- 关键词：`pytest` `Next.js` `Playwright` `DOUYIN_VERIFICATION_REQUIRED` `crawler`
- 状态：自动化已通过，真实平台链路待人工验证

### 背景与现象

执行当前工作区的全部可重复验证，包括后端测试、前端类型检查与生产构建、本地 API/Web 运行态、桌面端 UI 导航、移动端布局、文本模型连接和两个真实抖音采集 smoke。

### 根因

代码质量和本地运行态没有失败；真实 Playwright 搜索在已登录状态下被抖音安全验证页拦截。外部 `douyin-comments-crawler` 的默认地址 `http://127.0.0.1:8000` 当前没有服务。独立 smoke 在 API 占用同一持久化 Profile 时会遇到浏览器锁，因此必须先停止本项目服务再运行独立 smoke。

### 解决办法

- 先执行 `.venv\\Scripts\\python.exe -m pytest -q`、`npm run lint`、`npm run build` 和 `git diff --check`。
- 重启本地服务后检查 `/health`、`/ready`、`/api/douyin/status` 和 `/api/settings/test-llm`。
- 用无头 Chromium 遍历全部 15 个导航页，并检查移动端菜单、横向溢出、控制台错误和 HTTP 失败。
- 独立 Playwright smoke 只记录真实的 `LOGGED_IN` 与安全验证错误，不使用 Mock、空结果或绕过平台验证；crawler smoke 只记录真实的 disconnected 状态。

### 验证结果

- 后端：`131 passed`，仅 2 个 Alembic deprecation warnings。
- 前端：`npm run lint` 通过，`npm run build` 通过。
- UI：桌面端 `15/15` 页面、回复页 `7/7` 指标、控制台错误 `0`、HTTP 失败 `0`；移动端无横向溢出，菜单可打开，控制台错误 `0`。
- 运行态：API `/health=200` 且 `running`，`/ready=200` 且 `ready`，Web `/=200`；Playwright 浏览器 `running`、登录态 `LOGGED_IN`；DeepSeek 文本连接成功。
- 真实 Playwright 独立 smoke：登录态 `LOGGED_IN`，搜索返回 `DOUYIN_VERIFICATION_REQUIRED`，报告为 `data\\acceptance\\douyin-playwright-smoke-20260917-all-tests-standalone.json`。
- 外部 crawler smoke：健康状态 `disconnected`，报告为 `data\\acceptance\\douyin-crawler-smoke-20260917-all-tests-rerun.json`。
- 因为尚未完成安全验证后的真实视频、评论 ID/文本、系统入库关联和人工复核，真实商业化 E2E 仍不能标记为通过。

### 下次预防

生产构建后必须重启 Next 开发服务再做浏览器验收；独立 Playwright smoke 不得与持久化 Profile 的 API 浏览器并发运行。真实平台验收必须把登录、搜索、评论、入库和回复 DOM 核验分成独立证据步骤。

### 可复用资产

- `scripts\\douyin_smoke_test.py`
- `scripts\\crawler_smoke_test.py`
- `docs\\real-acceptance-matrix.md`

## 2026-09-17｜前后端接口对应性复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 文档修正
- 关键词：`API contract` `Next.js` `FastAPI` `read-only smoke`
- 状态：已验证（真实平台依赖仍待人工）

### 背景与现象

继续核对前端页面实际调用的请求路径与 FastAPI 路由，避免页面看似可用但请求落到不存在的接口。

### 解决办法

从 `web/app/page.tsx` 提取 44 个请求路径，与后端 49 个路由进行动态参数归一化比对；任务的 `pause/resume/retry` 动态动作按后端三个独立路由核对。随后对当前数据库的实际项目、视频、评论、潜客和任务 ID 执行只读 API 冒烟。

### 验证结果

- 前端请求路径无未实现接口；仅通用任务动作路径需要按动作名映射到已有路由。
- 只读 API 冒烟 `26/26` 通过，未执行扫描、写入、回复或发送操作。
- 文档已同步当前真实状态，并移除二级评论稳定能力的过度承诺。
- Docker 验证未执行：当前 Windows 环境未安装 Docker CLI；不能把 Compose 生产启动标记为已验证。

### 下次预防

新增前端请求时同时增加后端路由契约测试；动态 URL 不能只按字符串字面比较，必须将参数和动作映射纳入检查。

## 2026-09-17｜生产源码边界门禁复核

- 项目：opcweb / AI Lead Radar
- 类型：验证 / 安全边界
- 关键词：`MockProvider` `text-only` `git ls-files` `credentials`
- 状态：已验证

### 验证结果

对 Git 已跟踪的 65 个生产源码文件执行静态门禁：未发现 `MockProvider`、`MockLLMProvider`、`VisionProvider`、`ImageProvider` 或 `MultimodalProvider`；`.env` 与 `data/` 运行数据未被 Git 跟踪。该门禁不能替代真实抖音 E2E，但证明当前提交边界没有把这些实现或本地凭据纳入版本控制。

## 2026-09-17｜opcweb 文本模型边界修复提交

- 项目：opcweb / AI Lead Radar
- 类型：发布 / 验证
- 关键词：`git commit` `git push` `text-only` `GitHub`
- 状态：已验证

### 背景与现象

完成本轮纯文本 AI 边界、抖音安全验证识别、AgentRun 失败审计、前端状态展示和验收文档更新后，需要先提交并推送到 GitHub。

### 根因

工作区包含源码、测试、文档和全局工作台文件的待提交修改；本地运行数据、环境变量、浏览器 Profile、构建产物和日志不应进入远程仓库。

### 解决办法

- 先轮询后端测试与前端生产构建任务，确认通过。
- 仅暂存 Git 工作区中的源码、测试、文档和启动脚本；执行 `git diff --cached --check` 和敏感信息模式扫描。
- 创建提交 `d107322`，随后执行 `git push origin main`。

### 验证结果

- 提交成功：`d107322 Complete text-only AI lead radar validation`。
- 推送成功：远程 `github.com:skyconnfig/opcweb.git` 的 `main` 已从 `ad2d5c2` 更新到 `d107322`。
- 本次验收仍未把抖音安全验证、外部 crawler 和真实回复发送标记为通过。

### 下次预防

提交前固定检查暂存区文件、差异空白、凭据模式和远程分支；远程发布成功后再用 `git ls-remote origin main` 核对远程提交。

### 可复用资产

- `docs\\real-acceptance-matrix.md`
- `docs\\commercial-readiness.md`
- `start.ps1`

## 2026-09-17｜回复审核队列与前端批量操作修复

- 项目：opcweb / AI Lead Radar
- 类型：实现 / 验证 / 发布
- 状态：已验证并推送

### 背景与现象

前一轮完成了评论分析事件契约、知识库不足时进入人工审核队列，以及前端评论来源展示、批量回复、关键词分类和潜客 B/C 级筛选；本轮需要把已验证修改提交到 GitHub。

### 解决办法

- 回复决策在需要人工审核但没有 AI 文本时保留为 `WAITING_REVIEW`，允许人工填写安全文本后再发送。
- 扫描流程补充 `comment.analyzed`、`lead.detected`、`reply.generated` 事件。
- 回复队列展示来源评论、用户和视频，支持单条编辑、批量选择和发送前确认。
- 评论池增加待回复筛选，关键词分类按真实数据动态展示，潜客池增加 B/C 级筛选。

### 验证结果

- 后端：`133 passed`，仅有 2 个 Alembic 配置弃用警告。
- 前端：`npm run lint` 通过。
- 前端：`npm run build` 通过。
- `git diff --cached --check` 通过，暂存区敏感信息模式扫描无命中。
- 提交：`7f5d23a Improve review queue and lead radar UX`。
- 远程 `origin/main` 已核对为 `7f5d23a68fd3c01955bc144ee44c7c97e83cb4ba`。
- 真实抖音安全验证、外部 crawler 连接和真实回复 E2E 仍未通过，不将其标记为完成。

### 下次预防

前端新增批量或状态操作时，同步核对后端请求体契约并增加真实接口或服务层测试；发布前继续执行后端测试、前端 lint/build、差异检查和远程提交核对。
