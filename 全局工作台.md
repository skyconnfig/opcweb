# 全局工作台

## 文档定位

这是 workspace 的总入口，负责说明全局工作方式、命名规则和三个基础文档的分工。

新项目开始前，必须先阅读本文件、[全局复利与踩坑日志](./全局复利与踩坑日志.md) 和 [新项目SOP](./新项目SOP.md)。

## 三个基础文档的作用

| 文档                    | 作用                                     | 什么时候使用                             |
| ----------------------- | ---------------------------------------- | ---------------------------------------- |
| `全局工作台.md`         | 全局入口、命名规则、目录规则、文档索引   | 新项目开始前，以及需要判断文件放置位置时 |
| `全局复利与踩坑日志.md` | 沉淀可复用经验、问题、解决办法和预防措施 | 遇到问题、解决问题、完成重要验证后       |
| `新项目SOP.md`          | 新项目从立项到交付的标准步骤和验收门槛   | 每次创建新项目时，按步骤执行             |

## 命名规则

### 项目专属文件夹

- 每个新项目必须拥有一个专属文件夹。
- 项目文件夹默认直接放在 workspace 根目录。
- 默认命名格式：`项目名`，优先使用小写英文和短横线，例如 `douyin-ai-news`、`invoice-tool`。
- 项目名应稳定、简短、能表达项目用途；不使用空格、连续标点或无意义的 `new`、`test2` 等名称。
- 用户明确指定项目名称时，按用户指定名称执行。
- 同名项目需要区分时，使用 `项目名-YYYYMMDD` 或明确版本后缀。

### 项目内文件

- 源码文件使用项目既有语言和框架的惯用命名规则。
- 文档使用清晰的中文或英文名称，避免 `最终版`、`最新`、`真的最终版` 等不稳定后缀。
- 有时间属性的记录使用 `YYYY-MM-DD`，例如 `2026-09-16-复盘.md`。
- 脚本、测试、配置、素材和生成结果必须放在所属项目文件夹内，不散落在 workspace 根目录。

## 推荐项目目录

按项目实际需要创建，不要求每个项目都填满：

```text
项目名/
├── README.md          # 项目说明、启动方式、验收结果
├── src/               # 源码
├── tests/             # 测试
├── scripts/           # 项目专用脚本
├── docs/              # 项目文档和调研资料
├── assets/            # 项目素材
├── output/            # 可交付产物
└── logs/              # 项目运行日志和验证证据
```

## 文件归属原则

1. 能独立服务于某个项目的文件，放进该项目的专属文件夹。
2. 能服务多个项目、描述全局规则的文件，放在 workspace 根目录。
3. 临时文件、缓存、构建产物不得冒充正式项目文件；应放入项目内的临时目录或被 `.gitignore` 忽略。
4. 本规则从新项目开始执行，不自动移动或重构已有项目文件。

## 经验复利规则

- 解决了可复用的问题后，及时写入 `全局复利与踩坑日志.md`。
- 记录事实、原因、解决办法和验证结果，避免只写结论。
- 同一问题再次出现时，先搜索日志，再决定是否采用旧方案。
- 如果旧经验已失效，在原条目中标注状态和替代方案，不删除历史事实。
