# 新项目 SOP

## 适用范围

凡是需要新建一组源码、脚本、文档、素材或可交付产物的工作，都视为新项目，必须执行本 SOP。

## 开工前硬性要求

在编写新项目代码、素材或正式文档之前，必须完整阅读以下三个文件：

1. `全局工作台.md`
2. `全局复利与踩坑日志.md`
3. `新项目SOP.md`

未完成阅读和目录确认前，不得开始正式实现。

## 标准流程

### 1. 明确项目边界

- 写清项目名称、目标、交付物和不包含的内容。
- 确认这是新项目，而不是已有项目的增量修改。
- 为项目选择稳定、唯一的文件夹名称。

### 2. 创建专属文件夹

在 workspace 根目录创建项目文件夹，所有该项目的文件都放入其中：

```text
项目名/
├── README.md
├── src/
├── tests/
├── scripts/
├── docs/
├── assets/
├── output/
└── logs/
```

只创建实际需要的目录，不为形式完整而堆放空目录。

### 3. 建立项目入口

在项目文件夹内创建 `README.md`，至少写明：

- 项目目标和当前范围
- 目录说明
- 安装和启动方式
- 验证命令
- 当前已知限制
- 交付物位置

### 4. 先做最小可验证闭环

按照“先能运行，再扩展功能”的顺序推进：

1. 准备最小输入或测试数据。
2. 实现核心路径。
3. 执行构建、测试或实际运行验证。
4. 保存关键验证结果和失败日志。

不要在核心闭环未验证前扩展无关功能。

### 5. 遇到问题及时沉淀

问题解决后立即判断是否具有跨项目复用价值：

- 有复用价值：更新根目录的 `全局复利与踩坑日志.md`。
- 只与本项目有关：记录到项目的 `README.md` 或 `logs/`。
- 仍未解决：记录现象、已尝试方案和下一步，不伪装成已完成。

### 6. 交付前检查

- 所有项目文件是否都在专属文件夹内。
- README 是否能指导其他人启动和验证。
- 构建、测试或运行结果是否真实执行过。
- 输出文件是否放在项目的 `output/` 内。
- 敏感信息是否已移除或替换为占位符。
- 新增经验和踩坑是否已更新到全局日志。

## 完成标准

只有在“目录归属明确、核心闭环已验证、限制已说明、经验已沉淀”四项都满足后，项目才可标记为完成。
